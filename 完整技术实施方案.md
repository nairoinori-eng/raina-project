# 完整技术实施方案

## 脊柱侧弯患者居家康复训练智能交互系统

> **你是谁**：技术小白，交互设计专业  
> **怎么用这份文档**：从头到尾按顺序做，每一步都写了"为什么做"和"具体怎么做"  
> **编程方式**：你不写代码，把本文档中的 Prompt 复制给 AI，AI 帮你写  
> **Deadline**：2026-05-10（布展）

---

# 〇、先花 5 分钟理解这个系统

## 你的作品最终是什么样的？

一个观众走进你的展位，会经历这些：

```
① 进屋看展板（30秒~1分钟）
② （可选）手机扫投影右下角 QR 码上传 X 光片
③ 穿上智能背心（你在旁边帮忙调整），拿起手持按钮自己按下开始（约1分钟）
④ 面对投影看引导动画（60秒），系统同时采集静息基线，然后直接开始呼吸体验
⑤ 做施罗斯呼吸，看到粒子脊柱随呼吸从弯变直（2分钟）
⑥ AI 生成专属艺术海报 + 扫码带走（约1分钟）
⑦ 用户按按钮重置，回到待机状态等待下一位（海报常驻展示，不赶人）
```

## 这套系统由哪几个东西组成？

```
                    ┌─────── 你的电脑 ───────┐
                    │                        │
 智能背心            │  Three.js 前端          │        投影仪
 (传感器感知呼吸)     │  (把呼吸数据变成         │ HDMI → (观众看到
  │                 │   好看的粒子动画)         │        的画面)
  │ USB线           │                        │
  └────────────────→│  Flask 服务             │
                    │  (处理X光片、生成海报、   │
 手机(4G/5G)        │   提供上传/海报网页)      │
 (观众扫码上传X光片  │                        │
  和扫码保存海报)    │  Python 中间层          │
 公网隧道 →─────────→│  (读Arduino串口→WebSocket) │
                    │                        │
 手持按钮            │                        │
 (用户自己按下启动)   │                        │
  │ USB/HID ────────→│                        │
                    └────────────────────────┘
```

**一句话总结**：背心感知呼吸 + 用户按按钮启动 → 电脑算出动画 → 投影给观众看。手机通过公网隧道传 X 光片和取海报。

## 你需要做的全部事情，分 5 个阶段

| 阶段 | 你在做什么 | 产出 | 时间 |
|------|-----------|------|------|
| **Phase 1** | 买硬件（含手持按钮）、接电路、烧代码、改背心 | 穿上背心电脑能看到呼吸数据；按钮能被电脑识别 | 3/29→4/09 |
| **Phase 2** | 用 Vibe Coding 搭建 Three.js 粒子脊柱前端 | 按传感器→画面里的脊柱从弯变直 | 4/07→4/18 |
| **Phase 3** | 写 Flask 服务 + 补全5个环节 | 完整走一遍从头到尾的体验 | 4/14→4/25 |
| **Phase 4** | 穿着背心反复测试、调参数 | 体验流畅好看、不崩溃 | 4/25→5/05 |
| **Phase 5** | 搬到展厅布置 | 展览就绪 | 5/05→5/10 |

---

# 一、Phase 1：硬件

> **这阶段结束后**：你穿上背心，电脑屏幕上能看到 4 个数字在跟着你的呼吸变化。

---

## 1.1 买东西

> **为什么**：你需要传感器来感知呼吸、马达来引导呼吸、Arduino 来连接它们、背心来穿在身上。

**你要做的**：打开淘宝，搜下面的关键词，下单。

| 买什么 | 淘宝搜 | 几个 | 多少钱 | 它是干嘛的 |
|--------|--------|------|--------|-----------|
| Arduino Nano | "Arduino Nano V3.0 CH340" | 1块 | ¥15-25 | 小电脑，读传感器数据、控制马达 |
| 薄膜压力传感器 | "FSR402 薄膜压力传感器" | 4片 | ¥40-80 | 贴在背心里，感知胸廓因呼吸产生的压力变化 |
| 振动马达 | "扁平振动马达 纽扣式 10mm 3V" | 2个 | ¥6-10 | 震动提示观众"往这个方向吸气" |
| 战术背心 | "战术背心 MOLLE 前开 魔术贴 黑色" | 1件 | ¥40-60 | 传感器和马达的载体，观众穿的 |
| 10KΩ 电阻 | "1/4W 金属膜电阻 10K" | 4个 | ¥1 | 和传感器配合使用（电路原理不用管） |
| 1KΩ 电阻 | "1/4W 金属膜电阻 1K" | 2个 | ¥1 | 和马达配合使用 |
| S8050 三极管 | "S8050 三极管" | 2个 | ¥1 | Arduino 的引脚电流太小直接带不动马达，三极管帮它放大电流 |
| 迷你面包板 | "SYB-170 迷你面包板" | 1块 | ¥3 | 不用焊接就能搭电路的板子 |
| 杜邦线 | "杜邦线 公对母 20cm" | 20根 | ¥5 | 连接用的线 |
| 魔术贴线槽 | "自粘式理线带 1cm" | 1米 | ¥5 | 在背心里固定走线 |
| USB延长线 | "USB Mini 延长线 2米" | 1根 | ¥10 | Arduino 连电脑，要够长 |
| 便携路由器 | "便携WiFi路由器" | 1个 | ¥50-80 | ~~不急买~~ **已不需要（改用公网隧道）** |
| 手持按钮 | "USB Arcade Button 小号迷你款" 或 "USB 投币机按钮 小号" | 1个 | ¥30-80 | 用户自己按下启动体验，USB即插即用 |
| EVA 泡棉 / 瑜伽垫 | "EVA泡棉 3mm"或用废旧瑜伽垫/鼠标垫代替 | 1张 | ¥0-5 | 做传感器过渡垫（详见 §1.4 第2步）|
| 魔术贴条（窄款）| "魔术贴 条 1cm 宽" | 2米 | ¥6 | 口袋封口 + 背心内走线固定 |

**合计**：电子元件约 **¥215-370**（含手持按钮），背心改造材料大部分可零成本替代

> 💡 EVA 可用废旧瑜伽垫/鼠标垫，布料可用旧衣服剪。额外花费可能不到 ¥20。
> 💡 不再需要便携路由器和 iPad（改用公网隧道 + 手机扫码）。

**怎么确认做对了**：东西都到了。

---

## 1.2 接电路

> **为什么**：把传感器、马达和 Arduino 连起来，Arduino 才能读到传感器数据、控制马达。

**你要做的**（按顺序，一步步来）：

### 第1步：把 Arduino 插到面包板上

> **为什么**：面包板是一个不用焊接的实验板，Arduino 插上去之后就可以用杜邦线连接其他元件。

把 Arduino Nano 的两排针脚对准面包板中间的槽，按下去。

### 第2步：接地线和电源线

> **为什么**：所有电子元件都需要供电（5V）和接地（GND），这两条线是基础。

- 用一根杜邦线，从 Arduino 的 **GND** 引脚连到面包板侧边的 **蓝色轨道**（这是公共地线）
- 用一根杜邦线，从 Arduino 的 **5V** 引脚连到面包板侧边的 **红色轨道**（这是公共电源）

### 第3步：接第 1 个传感器（S1，左胸）

> **为什么**：这个传感器会贴在背心左胸位置，当观众做施罗斯呼吸时，左胸（塌陷侧）会膨胀，传感器压力增大 → Arduino 读到的数值就会变大。

薄膜压力传感器有两个引脚：
- 引脚 1 → 用杜邦线连到**红色轨道**（5V 供电）
- 引脚 2 → 同时连两个东西：
  - 用杜邦线连到 Arduino 的 **A0** 口
  - 在面包板上接一个 **10KΩ 电阻**，电阻另一端连到**蓝色轨道**（GND）

> 💡 这叫"分压电路"。原理不用管，照着接就行。

### 第4步：接另外 3 个传感器

跟第3步一模一样，只是连到不同的 Arduino 口：
- S2（右胸）→ **A1**
- S3（左腰）→ **A2**
- S4（右腰）→ **A3**

### 第5步：接第 1 个马达（M1，左胸）

> **为什么**：这个马达贴在背心左胸位置，通过震动提醒观众"把气吸到这里"。Arduino 不能直接带动马达（电流不够），所以需要一个三极管来帮忙放大电流。

三极管 S8050 有 3 个引脚（扁面朝你，从左到右是 E、B、C）：
- **E（发射极，最左）**→ 连到**蓝色轨道**（GND）
- **B（基极，中间）**→ 接一个 **1KΩ 电阻**，电阻另一端连到 Arduino 的 **D5** 口
- **C（集电极，最右）**→ 连到马达的**负极**（马达的**正极**连到**红色轨道 5V**）

### 第6步：接第 2 个马达

跟第5步一模一样，只是连到 Arduino 的 **D6** 口。

### 第7步：USB 连电脑

把 Arduino 用 USB 线连到你的电脑。

**怎么确认做对了**：Arduino 上的灯亮了（有电了），暂时不会有其他反应——需要烧代码之后才能测试。

---

## 1.3 给 Arduino 烧代码

> **为什么**：Arduino 是个空白的小电脑，需要你把程序"烧"进去，它才知道该做什么——读传感器、算数据、控马达、发数据给电脑。

### 第1步：安装 Arduino IDE

> **为什么**：这是给 Arduino 烧程序的官方软件。

打开 https://www.arduino.cc/en/software ，下载安装。

### 第2步：打开软件，配置

打开 Arduino IDE，做 3 个设置：
- 菜单 → **工具 → 开发板** → 选 **"Arduino Nano"**
- 菜单 → **工具 → 处理器** → 选 **"ATmega328P (Old Bootloader)"**
- 菜单 → **工具 → 端口** → 选带 **"usbserial"** 或 **"CH340"** 字样的那个

> 💡 如果端口菜单里没东西，说明 USB 没接好，或者需要装 CH340 驱动（搜"CH340 Mac驱动"下载装一下）。

### 第3步：让 AI 帮你写代码

打开 ChatGPT / Claude / WorkBuddy，**完整复制下面这段话**给它：

```
请帮我写一个 Arduino Nano 的完整 .ino 代码。

## 硬件连接
- 4个薄膜压力传感器：A0(左胸S1) A1(右胸S2) A2(左腰S3) A3(右腰S4)
- 2个振动马达（三极管驱动）：D5(左胸M1) D6(右腰M2)
- USB Serial 通信，波特率 115200

## 功能1：读传感器 + 发数据
- 每秒读30次传感器（30Hz）
- 每个传感器做5点滑动平均滤波（去除抖动噪声）
- 计算 blend 值：
  胸弯纠正 = S1(A0) - S2(A1)
  腰弯纠正 = S4(A3) - S3(A2)
  raw = 胸弯纠正 × W1 + 腰弯纠正 × W2
  B = constrain(map(raw, 0, THRESHOLD, 0, 255), 0, 255)
  
  权重 W1/W2 默认 0.6/0.4（对应设计文档中的 Wc/Ww，即 weightChest/weightWaist。W1=胸弯权重=Wc，W2=腰弯权重=Ww）
  如果电脑 Serial 发来 "WEIGHT:0.4,0.6" 之类的指令，就更新 W1/W2
  （Flask 会根据上传的X光片AI分析结果，动态计算权重发给Arduino）
  
  ⚠️ THRESHOLD 初始值先设 200，但这不是最终值！
  需要穿上背心现场标定：
    1. 平静呼吸，记下 raw 通常值（比如 30-50）
    2. 用力做施罗斯呼吸，记下 raw 最大能到多少（比如 180）
    3. 把那个最大值填进 THRESHOLD
  所以代码里 THRESHOLD 和 W1/W2 都做成容易修改的变量（写在代码顶部）
- 每次 Serial 输出一行：S1:xxx,S2:xxx,S3:xxx,S4:xxx,B:xxx

## 功能2：穿戴检测（已取消自动检测，保留基础监测）
> **更新**：不再做传感器自动就绪检测（由用户自己按手持按钮启动）。
> 以下代码仅用于 Arduino 本地调试时确认传感器工作正常，不需要发送 VEST_READY 信号。
- 4个传感器都 > 50 → 说明背心穿戴基本到位（调试参考，不触发前端状态变化）
- 任一传感器骤降到 < 30 → 可能是背心脱落或线路问题（调试参考）
- **实际流程**：用户穿好背心后自己按手持按钮 → Flask 发 START 指令给 Arduino → Arduino 开始体验模式

## 功能3：振动马达引导呼吸节奏
- 收到 "START" 后开始计时
- 呼吸周期10秒（4秒吸+2秒停+4秒呼，停顿对应施罗斯"撑住"阶段）
- 吸气开始前0.5秒，两个马达同时短振3下（PWM=200，每下50ms间隔50ms）
- 全程同一节奏振动，不减弱、不停止（正念引导惯例）
- 体验中如果 blend 连续10秒 < 30，轻振1次提醒

## 功能4：接收电脑指令
- "START" → 开始体验
- "MOTOR_OFF" → 关马达（上传了X光片的患者，弯型未知，关掉避免误导）
- "MOTOR_ON" → 开马达
- "WEIGHT:x.x,x.x" → 更新胸弯/腰弯权重（如 "WEIGHT:0.4,0.6" 表示腰弯为主弯）
- "RESET" → 全部状态归零（权重恢复默认 0.6/0.4）

请给我完整的、可直接编译上传的代码。加上详细注释。
```

### 第4步：粘贴代码，上传

- AI 会给你一段代码，**全选复制**
- 粘贴到 Arduino IDE 的代码编辑区
- 点左上角 **✓**（编译验证）→ 等它跑完，底部显示"编译完成"
- 点 **→**（上传）→ 等底部显示"上传完成"

> 💡 如果报错说"上传失败"，把第2步的处理器选项换成另一个试试。

### 第5步：测试

- 点 Arduino IDE 右上角的 **🔍**（串口监视器）
- 右下角波特率选 **115200**
- 你应该看到一行行数据在滚动：`S1:xxx,S2:xxx,S3:xxx,S4:xxx,B:xxx`
- **用手指按压传感器** → 对应的 S1-S4 数值应该明显变大
- 如果不变 → 检查接线

**怎么确认做对了**：串口监视器里能看到数据，按传感器数值会变。

---

## 1.4 改造背心

> **为什么**：把传感器、马达、Arduino 和线都装到背心里，变成一件可穿戴的设备。
> 
> **核心挑战**：FSR402 是平的，人的背部是弯的——如果处理不好，传感器读数会乱跳。所以这一节会详细讲怎么让传感器**充分贴合身体**。

### 第0步：准备背心改造材料

除了 §1.1 买的电子元件，你还需要：

| 材料 | 去哪弄 | 多少钱 | 干嘛用 |
|------|--------|--------|--------|
| EVA 泡棉 / 瑜伽垫 / 鼠标垫 | 淘宝"EVA泡棉 3mm"或家里找废旧的 | ¥0-5 | 做传感器过渡垫（解决"平传感器贴弧面背部"的关键） |
| 黑色布料（旧 T 恤也行） | 剪一件不穿的旧衣服 | 免费 | 缝传感器口袋 |
| 魔术贴条（1cm 宽） | 淘宝或文具店 | ¥6 | 口袋封口 |
| 薄棉布 / 旧 T 恤布 | 旧衣服剪 | 免费 | 过渡垫底面接触皮肤 |
| 手缝针线 | 家里找或有便利店买 | ¥0-8 | 缝口袋 |

> 💡 大部分材料家里就能找到。真正要花的可能只有魔术贴条 ¥6。

### 第1步：确定传感器位置

穿上背心，用粉笔在**背心内侧**标 4 个位置：
- **S1 左胸**：左侧肩胛骨下缘高度
- **S2 右胸**：右侧同样高度
- **S3 左腰**：左侧腰线（最细的地方）
- **S4 右腰**：右侧腰线

> ⚠️ 这 4 个位置要对准**肋骨区域**（有骨头支撑的硬表面），不是正脊柱中线（软组织呼吸起伏大但不稳定）。
> 
> 💡 这 4 个位置对应施罗斯呼吸法的关键监测区域。你自己是 S 型胸右凸+腰左凸，S1 和 S4 是你的塌陷侧。

### 第2步：制作海绵过渡垫（每个传感器一个，共做4个）

这是让传感器读数稳定的**关键一步**。

**为什么要这个垫子？**

```
问题：背部是弧面，平面传感器的中间会悬空

  背部:   ＿／‾＼＿    ← 弧形
  传感器: ───────      ← 平的
          ↑ 中间悬空！只有边缘碰到肉 → 读数不准

解决：加一层能变形的海绵填满缝隙

  背部:   ＿／‾＼＿
  海绵垫: ／～～～～＼  ← 海绵变形贴合弧面
  传感器: ╭──────╮   ← 坐在海绵中间的平面上，四周被托住
```

**做法（每个垫子约 30 秒）：**

```
① 从 EVA 泡棉/瑜伽垫上剪一个正方形
   尺寸：6cm × 6cm，厚度 3-5mm（就是泡棉原本的厚度）

   ┌──────────┐
   │          │  6cm × 6cm 的方块
   │          │
   └──────────┘


② 在中间挖一个圆孔
   直径约 3.8cm（比 FSR402 传感器稍微小一点点，能卡住就行）
   找个圆形的东西当模板（瓶盖、胶带圈都行）

   ┌──────────┐
   │  ╭────╯  │  中间的圆孔放传感器
   │  │    │  │  孔边缘托住传感器防止翘边
   │  ╰────╮  │
   └──────────┘


③ （推荐）底面粘/缝一层薄布
   从旧 T 恤上剪一块 6cm×6cm 的布
   固定在垫子的其中一面
   
   这一面朝向皮肤 → 柔软舒服

重复做 4 个，总共约 10 分钟
```

### 第3步：缝制传感器口袋

在每个标记位置做一个口袋来固定传感器+海绵垫组合。

**口袋规格：7cm × 7cm**（比传感器+垫子稍大，留余量方便调整和塞补偿棉布）

**两种方式任选：**

**方式 A——手缝/缝纫机（更牢固）：**
1. 剪 4 块 7cm×7cm 黑色布料
2. 三边缝在背心内衬标记位置上，留一边开口（开口朝上/朝头部方向）
3. 开口处缝一条 2cm 长的魔术贴当封口

**方式 B——布基胶带粘合（最快，5 分钟搞定）：**
1. 剪 3 条约 8cm 长的布基胶带（布面胶带，不是普通透明胶带）
2. 在目标位置围成一个三边框 = 口袋
3. 第 4 条胶带当可反复撕贴的封口

**装填顺序：**

```
口袋内部：
  ① 先放入海绵过渡垫（布面朝外 = 朝向皮肤方向）
  ② 再把 FSR402 传感器放进垫子的圆孔里
  ③ 整套塞进口袋
  ④ 魔术贴封口（不要封太紧，留一点余量）
  
  导线出口：口袋侧面开一个小缺口（约1cm宽），导线从这引出，
  缺口旁用魔术贴扎住导线防止回拉带动传感器移位
```

> 💡 **口袋做大一点的意义**：不同体型的人骨骼位置可能有 ±2cm 偏差，大口袋允许你在里面微调传感器位置。

### 第4步：固定马达

- M1 马达：在 S1 左胸旁边缝/粘一个 **15mm×15mm** 的小口袋，内部衬一层薄布缓冲
- M2 马达：在 S4 右腰旁边同样操作
- 马达放进去后用少量热熔胶或针线轻微定位（不需要完全封死，方便更换）

> ⚠️ 马达不能直接贴着骨头——震感太硬太吵。放在稍微有肉的舒适区。
> 
> 💡 马达放在塌陷侧（目标吸气方向），因为施罗斯的核心是"向塌陷侧吸气"，马达震的地方就是观众要把气吸过去的方向。
> 
> > ⚠️ 这只对你的弯型（胸右凸+腰左凸）是正确的。上传了 X 光片的其他患者弯型可能不同，个性化模式会自动关掉马达。

### 第5步：走线

- 所有杜邦线沿背心**内衬**走，用**魔术贴线槽**粘住固定
- 线汇集到**背心下摆**
- Arduino Nano 放在下摆的一个口袋里（或魔术贴粘在外侧）
- USB 线从下方引出，连到电脑

### 第6步：试穿调试

穿上背心 → 连电脑 → 打开串口监视器 → **先检查基础读数**：

**调试检查清单：**

```
□ 4个通道都有数据（不应该有0或恒定值）
□ 平静站立时数值基本稳定（允许小幅波动）
□ 用手按压某个传感器 → 对应通道数值明显变大
□ 松开后数值回到基础水平附近
```

**如果发现数据异常：**

| 问题 | 可能原因 | 解决方法 |
|------|---------|---------|
| 某个通道始终为0 | 接线松动/接触不良 | 重新插杜邦线 |
| 数值乱跳波动大 | 传感器没贴合好 / 背心太松 | 见下面的松紧调节 |
| 某个通道数值特别高 | 口袋里垫得太厚 / 背心太紧 | 取出一些补偿棉布 |
| 呼吸时数值不变 | 传感器完全没接触到身体 | 加厚过渡垫 + 收紧背心 |

**背心松紧调节（现场引导员操作）：**

战术背心的调节点：
- **肩带长度扣** → 收紧让上半身贴紧
- **侧面魔术贴** → 收腰让腰部贴紧

判断标准（用手摸检测）：
- ✅ **"一指松紧"**：手指勉强能平插进背心和后背之间 → 刚好
- ✗ **太松**：手指自由滑动，传感器碰不到背 → 继续收紧
- ✗ **太紧**：一根手指都插不进去，呼吸困难 → 放松一点

**特殊体型快速补偿：**

| 情况 | 方法 |
|------|------|
| 观众很瘦，背心调到最紧还有空隙 | 在口袋内传感器背后塞一片折叠的棉布，把传感器往外"顶"向皮肤 |
| 观众较壮 | 一般可调范围够用；极端情况备大号背心 |
| 观众穿了厚衣服 | 准备统一的基础紧身 T 恤让观众换穿（既卫生又保证数据一致性） |

> 💡 **展览技巧**：在肩带和侧面魔术贴位置做**标记线**，记录不同体型适合的调节位置，方便快速复现。

**最终确认：**

```
✅ 穿上背心连电脑后，串口能看到 S1-S4 数据且稳定
✅ 深呼吸时，数据有明显变化（尤其 S1 和 S4 应该变化更大）
✅ 马达能震动
✅ 穿脱方便，线不会扯断
✅ 不同人穿都能通过调节获得可用数据
```

**🎉 Phase 1 完成！你现在有了一件能感知呼吸的智能背心。**

---

# 二、Phase 2：投影画面

> **这阶段结束后**：你按压传感器（或穿背心呼吸），投影上的粒子脊柱从弯变直，颜色从蓝紫变暖金。

> **编程方式**：这一整阶段你都不需要写代码。把下面的 Prompt 复制给 AI（比如 ChatGPT、Cursor 等），AI 会帮你生成完整的代码文件。你只需要运行它们就行。
> 这种方式叫 **Vibe Coding**——你描述想要什么效果，AI 帮你写代码。

---

## 2.1 准备工作：安装 Node.js 和创建项目

> **为什么**：Three.js 是一个 JavaScript 库，需要在浏览器里运行。Node.js 用来管理项目依赖和启动开发服务器。

**你要做的**：
1. 打开 https://nodejs.org ，下载 LTS 版本（长期支持版），安装
2. 打开终端（Mac 按 `Command + 空格`，输入"终端"），进入你的工作目录：
   ```
   cd ~/WorkBuddy/20260405192940
   mkdir spine-viz
   cd spine-viz
   npm init -y
   npm install three vite
   ```
3. 创建基础文件结构（让 AI 帮你做也行）：
   ```
   spine-viz/
   ├── index.html          ← 浏览器打开这个
   ├── style.css           ← 样式
   └── main.js             ← Three.js 粒子脊柱代码（AI 写）
   ```

---

## 2.2 把 Arduino 数据接到网页上

> **为什么**：Phase 1 里 Arduino 在不断发传感器数据，现在要让浏览器能接收到这些数据，才能用呼吸驱动视觉。
> 数据路径：Arduino → USB → Python(pyserial)读取 → Flask WebSocket → 浏览器(WebSocket)

### 步骤1：确认 Arduino 正在发送数据

- 先别关掉 Arduino IDE 的串口监视器
- 确认你能看到类似 `S1:xxx,S2:xxx,S3:xxx,S4:xxx,B:xxx` 的数据在滚动（4个传感器值 + blend 值）

### 步骤2：让 AI 帮你写 Python 中间层

复制以下 Prompt 给 AI：

```
请帮我写一个 Python 文件 serial_bridge.py，功能如下：
- 用 pyserial 读取 Arduino Nano 的串口数据（波特率 115200）
- 数据格式是每行 "S1:xxx,S2:xxx,S3:xxx,S4:xxx,B:xxx"
- 解析出4个传感器值，计算 blend 值：
  胸弯纠正 = S1 - S2，腰弯纠正 = S4 - S3
  raw_score = 胸弯纠正*0.6 + 腰弯纠正*0.4
  blend = clamp(raw_score / 阈值, 0, 1) （阈值先设为200，需现场标定）
- 用 Flask-SocketIO 把 blend 值实时推送到浏览器
- 提供一个简单的 HTML 页面显示当前 blend 值（用于调试）
```

AI 生成的文件保存为 `flask_server/serial_bridge.py`，然后运行它测试：
```
cd flask_server
pip install flask flask-socketio pyserial
python serial_bridge.py
```
浏览器打开 http://localhost:5000 ，你应该能看到 blend 值在变化。

---

## 2.3 搭建粒子脊柱（核心视觉）

> **这是整个作品的视觉核心**——一条由粒子构成的脊柱曲线，形状随呼吸在"弯曲"和"变直"之间变化。
> 你不需要懂 Three.js，直接让 AI 帮你写。

### 复制这个 Prompt 给 AI：

```
我正在用 Three.js 做一个交互式粒子脊柱可视化项目。请帮我写完整的 main.js 代码。

需求：
1. 用 Three.js (ES Module 方式 import) 创建场景
2. 粒子脊柱：
   - 定义一条 S 型弯曲脊柱曲线（19个控制点，用 CatmullRomCurve3）
   - 定义一条直线脊柱曲线（同样19个点，x坐标全为0）
   - 根据 blend 参数（0~1）在这两条曲线之间插值（lerp）
   - 在曲线上分布 1200 个粒子（Points + BufferGeometry + ShaderMaterial）
   - 粒子不是硬边圆点，而是柔和的发光圆形（用 radial gradient texture 或 shader 实现）
   - 粒子有 ±0.05 随机偏移形成"带状"分布
3. 颜色系统：
   - blend=0 时深蓝紫 #2d1b69
   - blend=0.25 时蓝紫 #4a2d8f
   - blend=0.5 时浅紫 #7b4fb5
   - blend=0.75 时紫金过渡 #b48264
   - blend=1.0 时暖金 #d4a24c
   - 颜色根据 blend 值平滑过渡
4. 背景：径向渐变，中心深蓝紫 #0a0a1a 到边缘纯黑 #000000
5. 环境微粒：200-300 个漂浮光点（1-3px，透明度10%-30%）
6. 后处理：Bloom 发光效果（用 UnrealBloomPass）
7. 通过 WebSocket 接收 blend 值（Socket.IO client），实时更新粒子形态和颜色
8. 响应窗口大小变化
9. 全屏显示（适合投影输出）

技术要求：
- 使用 Vite 作为构建工具（已安装 three 和 vite）
- import * as THREE from 'three'
- import { io } from 'socket.io-client'（连接 localhost:5000）
- 粒子材质用 ShaderMaterial 或 PointsMaterial（选更合适的方式）
- Bloom 使用 EffectComposer + RenderPass + UnrealBloomPass
```

AI 生成的代码保存到 `main.js`，然后在终端运行：
```
npx vite --host
```
浏览器打开 http://localhost:5173 ，你应该能看到一条发光的粒子脊柱！

---

## 2.4 让画面响应呼吸

> **为什么**：上一节搭好了静态的粒子脊柱，现在要让它动起来——呼吸时从弯变直。

### 如果 2.2 和 2.3 都做对了

这时候你应该已经能看到了：
- **Python 中间层**正在从 Arduino 读数据并通过 WebSocket 推送 blend 值
- **Three.js 页面**通过 WebSocket 接收 blend 值并更新粒子

试着按压传感器（或穿上背心做施罗斯呼吸），粒子脊柱应该会跟着变化！

### 如果没有反应

检查这几步：
1. Python 中间层在跑吗？终端有没有报错？
2. 浏览器控制台有没有报错？（按 F12 打开开发者工具）
3. 把报错信息复制给 AI："我在运行 Three.js + SocketIO 的项目，遇到这个报错，帮我修复"

---

## 2.5 视觉调优

> **为什么**：基础效果有了，但可能还不够好看。这一步是打磨细节。

### 复制这个 Prompt 给 AI：

```
我的 Three.js 粒子脊柱项目已经有基本效果了，帮我优化以下方面：

1. 粒子效果：
   - 让粒子看起来更像柔和发光的光点（不是硬边圆圈）
   - 加一点微微的呼吸脉动（粒子大小随时间 ±10% 变化，周期约3秒）
   - 每个粒子加极微弱的随机位移（Noise），让它不完全静止，有生命感

2. 颜色渐变更平滑：
   - 当前颜色跳变太明显了，希望过渡更自然
   - 粒子亮度也跟随 blend 变化（blend=0 时亮度0.6，blend=1 时亮度1.2）

3. 后处理优化：
   - Bloom 效果：blend 低时 Bloom 弱，blend 高时 Bloom 强
   - 加一点轻微暗角（Vignette）

4. 环境微粒增强：
   - 正确呼吸时微粒微微向脊柱方向聚拢
   - 微粒颜色跟随主色调但更暗淡

5. 性能：
   - 确保 60fps 流畅运行
   - 粒子数控制在 1500 以内（脊柱1200 + 环境300）
```

---

## 2.6 怎么确认全部做好了？

```
✅ 按压传感器 → 投影中的粒子脊柱从弯变直
✅ 松开 → 脊柱慢慢弯回来（不是瞬间弹回去）
✅ 弯曲时蓝紫色，变直时暖金色，过渡平滑
✅ 有发光效果（Bloom），画面好看不卡
✅ 环境微粒在缓慢漂浮
✅ 全屏模式可以投到投影仪上
```

**🎉 Phase 2 完成！核心视觉效果做出来了。**

---

# 三、Phase 3：把 5 个环节串起来

> **这阶段结束后**：从头到尾跑一遍完整的观众体验——引导→穿戴→呼吸→AI生图→扫码。

---

## 3.1 搭 Flask 服务（后台大脑）

> **为什么**：需要一个 Python 服务来处理手机上传网页、调 AI 接口、生成二维码（动态刷新），同时作为 Arduino 和浏览器之间的数据桥梁。手持按钮的信号也通过这里转发。

### 第1步：确认 Python 可用

打开终端（Mac 的 Terminal 应用），输入 `python3 --version`，看到 3.x.x 就OK。

### 第2步：安装依赖库

终端输入：
```
pip3 install flask flask-socketio pillow qrcode google-genai pyserial eventlet
```

### 第3步：获取 Gemini API Key

> **为什么**：AI 脊柱坐标提取和海报生成都需要调 Google 的 Gemini API，Key 是你的"通行证"。

- 打开 https://aistudio.google.com/apikey
- 登录 Google 账号
- 点 Create API Key → 复制保存好

### 第4步：让 AI 帮你写 Flask 代码

把下面这段 Prompt 完整复制给 AI：

```
请帮我写一个完整的 Python Flask + SocketIO 服务（app.py），用于脊柱侧弯交互装置的后台。

## 系统架构
- 前端：Three.js 浏览器页面（通过 SocketIO 接收数据和指令）
- 后端：Flask-SocketIO 服务（Python）
- 硬件：Arduino Nano 通过 USB 串口连接（pyserial 读取）

## 功能清单

### 1. Arduino 数据桥接
- 用 pyserial 读取 Arduino 串口数据（波特率115200），格式 "S1:xxx,S2:xxx,S3:xxx,S4:xxx,B:xxx"
- 解析传感器值（4个）并计算 blend 值：
  胸弯纠正 = S1 - S2，腰弯纠正 = S4 - S3
  raw_score = 胸弯纠正*Wc + 腰弯纠正*Ww   （权重 Wc/Ww 默认 0.6/0.4，支持通过 WEIGHT 指令动态调整）
  blend = clamp(raw_score / 阈值, 0, 1)
  （也可直接使用 Arduino 已计算的 B 值）
- 通过 SocketIO 将 blend 值实时推送给所有连接的浏览器客户端

### 2. 手机上传网页（X光片上传，公网访问）
- GET /upload → 移动端优化的上传页面（竖屏适配）：
  - "点击拍照或选择相册"按钮
  - "上传"按钮
  - 极简设计：只做上传+结果反馈
- POST /upload → 接收X光片图片，调 Gemini API 提取脊柱坐标
  - 将结果存入当前 session（绑定 session_id）
  - 返回 JSON 给手机端：「{success: true, message: "已上传，请按开始按钮"}」或失败提示
  - **不在手机端显示分析结果**（脊柱类型、Cobb 角等在投影引导阶段展示）

> **注意**：上传页面的 URL 需要通过公网隧道（ngrok/Cloudflare Tunnel）暴露。QR 码中携带当前 session_id 参数。
> 每轮体验结束后 session 销毁、QR 码刷新，旧链接失效。

### 3. 手持按钮信号接收
- 手持按钮通过 USB 被电脑识别为键盘输入（HID）或 Arduino 串口信号
- 当检测到按钮按下时：
  - 锁定当前 session（不再接受新的 X 光上传）
  - 通过 SocketIO 广播 start_guide 事件给前端
  - 同时通知 Arduino 进入体验模式（START 指令）

### 3. Gemini 脊柱坐标提取
- 模型：gemini-2.0-flash
- Key 从环境变量 GEMINI_API_KEY 读
- Prompt：分析X光片，沿脊柱中线从C7到S1标记17-19个椎体中心点，
  返回 JSON {points:[{label,x,y}]}，x/y归一化到-1~1
- 结果通过 SocketIO 发送给前端更新粒子曲线

### 4. AI 海报生成
- 前端体验结束后通过 POST /generate 发送数据
- 根据 blend_final/breath_count/avg_intensity 构造 prompt
- 调用 Gemini API 生成 1024×1024 图片
- 保存到 posters/ 目录
- 生成后通过 SocketIO 广播 poster_ready 事件

### 5. 二维码 + 图片托管（公网访问）
- GET /poster/<name> → 返回海报图片
- GET /view/<name> → 深色手机端页面，大图+提示"长按保存"
- 生成二维码（qrcode 库），通过 SocketIO 发送 qr_data_url 给前端显示
- **两个不同的 QR 码**：
  - 待机时的 QR 码：链接到上传页面 `/upload?session=xxx`（投影右下角显示）
  - 结束时的 QR 码：链接到海报页面 `/view/xxx`（投影全屏显示）
  - 每轮体验结束后两个码都自动刷新

### 6. 状态管理 + 自动重置
- GET /status → 返回当前系统状态JSON
- POST /reset → 重置所有状态：
  - 销毁当前 session、创建新 session
  - 清空 X 光数据
  - 刷新上传 QR 码和海报 QR 码
  - 广播 reset 事件 → 前端回到 IDLE 待机态
- **按钮重置**：第五阶段（扫码带走）**常驻展示不设倒计时**，用户按下手持按钮后前端调用 POST /reset 触发重置。设计者也可手动调用 reset（展览开始前/结束后）

### 7. 公网隧道配置
- 推荐使用 **Cloudflare Tunnel**（免费，需注册 Cloudflare 账号）或 **ngrok**（免费，需注册账号）
- 将本地 Flask 服务的 `/upload` 和 `/view` 路由暴露到公网
- 手机使用 4G/5G 即可访问，不需要连 WiFi 或局域网
- 配置方法（展前执行一次）：
  ```
  # Cloudflare Tunnel 方式：
  cloudflared tunnel --url http://localhost:5000
  
  # 或 ngrok 方式：
  ngrok http 5000
  ```
- 终端输出的公网 URL 填入 Flask 配置的 `PUBLIC_URL` 变量

## 技术要求
- Python 3，使用 flask + flask-socketio + pyserial + pillow + qrcode + google-genai
- SocketIO 用 eventlet 作为异步模式
- 监听 0.0.0.0:5000
- 启动时自动获取并打印局域网IP
- 所有API调用加 try-except
- Arduino 串口连接失败时给出友好提示但不崩溃

请给完整可运行的代码，加注释。
```

### 第5步：启动服务

```
终端：
cd 到代码目录
export GEMINI_API_KEY="你的Key"
python3 app.py
```

终端会显示 `Running on http://0.0.0.0:5000`。浏览器打开那个地址就是投影页面。
手机上传页面通过公网隧道 URL 访问（展前配置好 ngrok/Cloudflare Tunnel）。

## 3.2 在前端补：认知引导动画（60 秒）

> **用户按下手持按钮后看到的引导内容**——情感共鸣 + 疾病介绍 + 呼吸教学。
>
> 详细脚本见 **设计方案_②认知引导.md §3**。

复制这个 Prompt 给 AI：

```
请在我的 Three.js 粒子脊柱项目中增加认知引导动画功能。

需求：
这是一个 60 秒的引导动画，分为 4 段（实际为 3 个内容段 + 1 个短过渡段）。在主体验之前播放。

整体机制：
- 增加一个状态机（state machine）：IDLE(待机) → GUIDE(引导) → EXPERIENCE(体验) → TRANSITION(过渡) → SHOWING(扫码展示)
- 默认状态为 IDLE，收到 'start_guide' 事件后进入 GUIDE 状态
- 引导结束后自动进入 EXPERIENCE 状态
- 引导阶段传感器继续采集基线数据（不驱动视觉，只记录）

渲染架构（混合架构）：
- Three.js canvas 层（底层）：粒子脊柱、高亮效果、对比线
- SVG/Canvas 叠加层（中层）：人体背面轮廓、胸廓膨胀动画
- HTML/CSS 叠加层（顶层）：发光圆点、箭头、呼吸节拍器、文字字幕

第1段（0:00-0:12）——「我的脊柱」情感共鸣：
- 不黑屏，直接进画面
- 粒子凝聚成微微弯曲的脊柱 → 逐渐变成明显 S 弯
- CSS 文字淡入淡出："这是我的脊柱。" / "15岁那年，我发现它弯了。"

第2段（0:12-0:22）——「什么是脊柱侧弯」+ 个人数据：
- Three.js：按 Y 坐标筛选粒子分段高亮（胸椎→暖橙，腰椎→青）
- Three.js：新增半透明正常脊柱对比线
- CSS 文字：疾病描述句
- CSS 文字：个人数据（有X光=用户Cobb角 / 无X光=示例数据28°/18°）

第3段（0:22-0:55）——「怎么呼吸」教学 ★核心段落，33秒：
  ③-a (0:22-0:32) 定位凹陷侧（施罗斯呼吸的目标位置）：
    - 显示人体背面 SVG 轮廓（叠加层 fade in）
    - 凹陷位置发光圆点 + CSS pulse 动画
    - 箭头位移动画（凸起→凹陷）
    - 文字："首先找到你背部凹陷的那一侧"
    - 原理：施罗斯呼吸要把气送到被挤压的凹陷侧，让被挤扁的肋骨重新撑开

  ③-b (0:32-0:45) 演示 + 跟做：
    - 胸廓单侧膨胀 Canvas 动画（只有凹陷一侧动——被撑开）
    - 呼吸节拍器 div（CSS scale 循环：吸气3s→撑开1.5s→呼气1.5s，循环2次）
    - 分步文字指令（吸气→撑开→呼），循环两次完整呼吸
    - 音效同步

  ③-c (0:45-0:55) 预告：
    - 教学元素 fade out
    - 回到粒子脊柱（微微变直 + 微偏暖色调）
    - 粒子场开始扩散

第4段（0:55-1:00）——「开始」过渡：
- 扩散完成 = 体验主体初始状态
- 文字："准备好了吗？" → "开始。"
- 状态切换到 EXPERIENCE

技术要求：
- HTML/CSS 叠加层显示文字和 UI 元素（不用 Three.js TextGeometry）
- CSS animation 淡入淡出 + pulse + scale 动画
- Timer 用 requestAnimationFrame（精确到帧）
- 字号至少 32px（投影观看距离考虑）
- 教学用的 2D 元素全部用 HTML/CSS/SVG/Canvas 实现，不占用 Three.js 渲染
```

## 3.3 在前端补：体验三段式节奏

> **不能 2 分钟都一个感觉**。前面要快速建立信心，中间自由呼吸，结尾推向高潮。

复制 Prompt 给 AI：

```
请在 Three.js 项目中实现体验三段式节奏控制。

三段式设计：
【教学期】0-30秒：灵敏度×1.5（让用户觉得"我做对了"）
【自由期】30-90秒：灵敏度渐变回1.0 + 衰减机制（停了慢慢退，不"白做"）
【高潮期】90-120秒：自动叠加+0.2（保证每个人都能看到脊柱变直）

呼吸引导（正念引导模式，全程不渐弱）：
- 0-5s: 显示开篇文字 "吸气 → 凹陷的一侧" (CSS fade in/out, 底部居中)
- 0-115s: 播放呼吸引导音效轨（4s吸气→2s停→4s呼循环，统一音量~0.6）。用 Web Audio API Oscillator 或 AudioBuffer
- 马达全程同一节奏不变（不减弱、不停止）
- ~115s: 呼吸引导音效 fade out (~3-5s)
- 120-138s: 正念收束语分步显示（**先静3s**→"慢下来……"→"最后一次深呼吸……"→"旅程结束。/谢谢你和它一起呼吸。"）。总~18秒，v5

技术要求：
- JavaScript 类 ExperienceController 管理
- requestAnimationFrame 计时判断阶段
- displayBlend 是最终传给渲染的值
- 120秒结束时发 'experience_end' 事件
```

## 3.4 在前端补：结束过渡和 AI 等待

> **为什么**：体验结束后不能突然黑屏。需要优美的过渡（粒子消散），然后等 AI 生成海报。

复制 Prompt 给 AI：

```
请在 Three.js 中实现结束过渡动画（体验120秒结束后触发，采用正念冥想收束结构 v5）：
【粒子消融】~18s：粒子从曲线分布变球形随机，透明度渐减到0（极缓慢，像冰融化）
【静默期】T+0~3s：纯画面无文字，粒子定格，让用户先消化
【正念收束语】分4步显示（每步间隔3s）：
  T+3s: "慢下来……" (fade in, 停留3s, fade out)
  T+9s: "最后一次深呼吸……" (同上)
  T+15s: "旅程结束。" (fade in)
        "谢谢你和它一起呼吸。" (停留4s, fade out)
  T+18s: 收束完成
【呼吸引导音效 fade out】T+0s开始引导音轨渐弱至0（~5s），文字开始时已接近消失
【光团等待】收束完成后粒子汇聚成发光团+脉动
【AI图展开】收到 poster_ready 后海报从中心圆形展开
新增 TRANSITION 和 WAITING 状态。超20s没收到数据显示"正在生成中…"。
```

## 3.5 在前端补：扫码页面

> **为什么**：把海报和二维码展示在投影上，让观众扫码保存。

海报图居中展示，二维码放在下方，底部文字"扫码保存你的专属艺术图"和"体验结束后请按按钮"。**不设倒计时，常驻展示。**
重置：用户按下手持按钮 → 收到 SocketIO button_press 事件（仅在 SHOWING 阶段响应）→ 发送 RESET 请求给 Flask → 回到 IDLE 状态（见 3.6 状态机）。

```
Prompt 要点：
- 海报=PlaneGeometry+MeshBasicMaterial贴图
- 二维码=收到qr_data_url(base64)后创建Texture贴图  
- 文字=HTML/CSS叠加层(DOM元素覆盖在canvas上方)
- 不设倒计时，不赶观众
```

## 3.6 在前端补：流程总控（状态机）

> **为什么**：需要一个"导演"来控制现在该播什么画面。

6 个状态的切换逻辑：

| 状态 | 画面 | 触发条件 | 下一个 |
|------|------|---------|--------|
| **IDLE 待机** | 散乱低密度粒子场（蓝紫灰调，慢漂移）+ 作品名称文字（居中，占位）+ 右下角上传 QR 码 + 提示文字 + 极低音量环境音。详见 **①§2.5** | 用户按手持按钮 (start_guide) → 名称/QR码淡出(~300ms)→粒子开始向脊柱凝聚→进入GUIDE。过渡算在60s内。详见 **②§5.6** | GUIDE |
| **GUIDE 引导** | 60s文字+脊柱演示（已穿背心，传感器采集中） | Timer=60s | EXPERIENCE |
| **EXPERIENCE 体验** | 传感器驱动+三段式 | Timer=120s | TRANSITION |
| **TRANSITION 收束过渡** | 粒子消融(~18s正念收束)→光团脉动等待 | 收束完成(138s) + poster_ready? → 如果AI已返回则直接跳到SHOWING；否则进入WAITING | WAITING / SHOWING |
| **WAITING 等待AI生成** | 光团持续脉动 + 文字"正在生成中…"（超20s才显示提示） | poster_ready 事件 | SHOWING |
| **SHOWING 展示+扫码** | 海报+新QR码（与上传码不同）。常驻展示，不设倒计时。 | **用户按手持按钮** → reset → IDLE |

> **关键变更（v3 定稿）**：IDLE 触发从"iPad点开始"改为"用户按手持按钮"；QR 状态从1个拆分为2个（待机上传码 + 结束海报码）；SHOWING 阶段海报+QR码**常驻展示，不设自动倒计时**，用户扫码后**按下手持按钮**才触发 reset 回 IDLE。

```
Prompt: 请实现AppStateManager类，
每个state有enter()/update(dt)/exit()方法，
主循环调用currentState.update(dt)，
SocketIO事件统一在Manager中处理。
```

## 3.7 音频

> **为什么**：舒缓的背景音乐增强沉浸感。

下载空灵氛围音乐（MP3/WAV）→ assets/audio/ambient.mp3

```
Prompt要点：
- Web Audio API (Audio对象或Howler.js)
- 双轨音频结构：
  轨① 背景音乐：音量随阶段变化（引导期0.3→体验期0.5-0.7→高潮期0.8-1.0→扫码期0.5）
  轨② 呼吸引导音效（新增）：4s吸+2s停+4s呼循环，统一音量~0.6，全程不渐弱
    ~115s开始fade out收束，配合正念收束语出现。详见 ③§5
- 用户首次交互后才播放（浏览器autoplay限制）
- 可选：轨①两轨音频(cold/warm)做crossfade
```

## 3.8 重置机制（按钮触发 + 常驻展示）

> **v3 更新**：不再使用 30 秒自动倒计时。SHOWING 阶段海报和 QR 码**常驻展示**，用户扫码保存完成后**自己按下手持按钮**才触发重置。

**为什么改**：
> - 用户扫码可能需要时间（找手机、对焦、保存）
> - 有人想多看几眼海报
> - 让用户掌控节奏，体验更好

重置流程：
1. Flask 收到按钮 RESET 信号（用户在 SHOWING 阶段按下手持按钮）
2. 销毁当前 session，生成全新 session_id
3. 清空 X 光数据和脊柱坐标
4. 刷新上传 QR 码（新的 session_id）→ 投影右下角显示新码
5. SocketIO 广播 `reset` 事件
6. 前端回到 IDLE 待机态 → 粒子恢复漂浮
7. Arduino 回到待机模式
8. 下一轮参观者看到的是**完全干净的系统**

**不需要任何人手动操作（用户按按钮即可）。** 全天无人值守循环。
唯一需要人工介入的情况：系统卡死/崩溃 → 刷新浏览器页面即可恢复；或者管理员可在电脑端手动触发 reset。

## 3.9 搭 Phase 3 遇到困难？复制这个 Prompt 给 AI

```
我在做一个基于 Three.js + Flask-SocketIO + Arduino 的脊柱侧弯交互装置。
我已经完成了 Phase 2（基础粒子脊柱能响应 blend 值变化）。
现在要把完整5环节流程串起来，请手把手教我。

【要加的东西】
1. 认知引导动画（60s：情感共鸣12s + 疾病介绍10s + 呼吸教学33s + 过渡5s。混合渲染架构：Three.js+SVG+HTML/CSS叠加层。教学段含呼吸节拍器+跟做练习×2次）
2. 体验三段式（0-30s灵敏度×1.5，Lag衰减rise=0.1/fall=2.0，最后30s自动+0.15）
2b. **呼吸引导**：开篇文字(0-5s)+全程呼吸引导音效(轨②,4s吸+2s停+4s呼循环,统一音量不渐弱)+马达全程不变+正念收束语(~18s分步告别v5)
3. **blend 保底机制**：blend 有最小增速 0.002/秒（全程生效），高潮期额外 +0.005/秒。传感器是主角但不会让 blend 卡在 0。不做弹窗提示。
4. **Session 随机种子**：每个 session 进入 EXPERIENCE 时生成 `Math.random()` 种子，Noise/粒子分布用 `(seed + time)` 做输入，保证同样 blend 的两次体验视觉细节不同
5. 结束过渡（粒子消融→光团等待→AI图展开）
6. 扫码页面（海报+二维码+**按钮重置，不设倒计时**）
7. 总控状态机（IDLE→GUIDE→EXPERIENCE→TRANSITION→SHOWING→**按钮reset**→IDLE）
8. 背景音乐（Web Audio API）
9. 自动重置（**按钮触发 reset 回 IDLE，SHOWING 阶段常驻展示不设倒计时**）

通信方式：
- 前端 ↔ Flask: SocketIO (socket.io-client)
- Flask ↔ Arduino: pyserial (USB串口)

请给我完整的代码方案。
```

**怎么确认做对了**：

```
✅ 用户按手持按钮 → 投影播认知引导（60s 文字+脊柱动画）
✅ 引导结束 → 自动切到体验（传感器驱动）
✅ 呼吸 → 脊柱变直 + 颜色变暖
✅ 2分钟到 → 粒子消融 → AI 生海报
✅ 海报+新二维码出现在投影上
✅ 手机扫码能打开并保存
✅ 用户按按钮 → 自动重置回待机，下一位可开始（**按钮触发，不设倒计时**）
```

**🎉 Phase 3 完成！完整体验流程跑通了。**

---

# 四、Phase 4：反复测试和打磨

> **这阶段结束后**：体验流畅、视觉精致、连续运行不崩溃。

## 你要反复做的事

### 穿上背心走完整流程

每次测试完对照这个清单打勾：

```
□ 穿上背心 → 用户自己按手持按钮 → 引导动画开播 + 基线开始采集
□ 引导结束 → 无缝切到体验
□ 做呼吸 → 脊柱变直 + 颜色变暖
□ 2分钟到 → 消融 → AI 生图
□ 扫码保存成功
□ 按按钮后重置回待机（**按钮触发，不设倒计时**）
```

### 调参数（需要实测才能确定）

```
□ 传感器差值阈值——多大的呼吸才算"有效"
□ blend 上升/下降速度——太快太慢都不好
□ 灵敏度 1.5 是否合适——可能要调成 1.3 或 2.0
□ 高潮叠加 0.2 是否合适——太大会假，太小没效果
□ 马达振动强度——太强打扰沉浸，太弱感觉不到
□ Bloom 强度——太亮过曝，太暗没发光感
```

### 常见问题速查

| 问题 | 解决 |
|------|------|
| 浏览器收不到 blend 数据 | 检查 Flask-SocketIO 服务是否在跑；检查 Arduino 串口是否被占用 |
| 传感器数据不变 | 检查杜邦线是否插紧 |
| 粒子脊柱不动 | 检查 SocketIO 连接是否建立（F12 控制台看日志） |
| 手机打不开上传页面 | 检查公网隧道（ngrok/Cloudflare Tunnel）是否在运行；确认手机用 4G/5G 而不是 WiFi |
| 手持按钮按下没反应 | 检查 USB 连接；检查电脑是否识别到 HID 设备或串口 |
| AI 报错 | 重新设环境变量 export GEMINI_API_KEY=... |
| 页面卡顿 | 减少粒子数 / 关闭开发者工具 / 确认用独立显卡 |

---

# 五、Phase 5：布展

## 展前准备清单

```
□ 背心、传感器、马达都OK
□ 手持按钮 USB 连接正常，按下能被电脑识别
□ Flask 服务和 Three.js 前端能正常运行
□ 公网隧道配好（ngrok/Cloudflare Tunnel），手机用 4G/5G 能打开上传页面
□ 手机扫码上传 X 光片测试通过
□ 投影/大屏调好
□ 展板印好
□ 预生成10张备选海报（断网降级用）
□ 完整流程走一遍无问题
□ 自动重置验证（一轮结束后确认回到干净待机态）
```

## 展览当天启动顺序

```
第1步：电脑联网（AI 功能需要外网）
第2步：终端启动公网隧道（cloudflared tunnel --url http://localhost:5000 或 ngrok http 5000）
   → 记下输出的公网 URL
第3步：终端启动 Flask（cd flask_server → export Key → export PUBLIC_URL="公网URL" → python3 app.py）
第4步：终端启动前端（cd spine-viz → npx vite --host）
第5步：插上手持按钮 USB，确认被电脑识别
第6步：确认 Flask 日志有 Arduino 数据
第7步：浏览器全屏（F11 或 presentation mode）→ 确认 HDMI 投影输出正常
第8步：用手机 4G/5G 扫投影上的 QR 码测试上传功能
第9步：走一遍完整流程（包括自动重置验证）
第10步：开展！
```

---

# 附录：项目文件结构

```
项目目录/
├── arduino/
│   └── spine_vest.ino          ← Arduino 代码
├── spine-viz/                  ← Three.js 前端项目
│   ├── index.html              ← 主页面
│   ├── style.css               ← 样式
│   ├── main.js                 ← 三维渲染+状态机（AI 写）
│   ├── package.json            ← 依赖配置
│   └── assets/
│       └── audio/              ← 背景音乐
├── flask_server/
│   ├── app.py                  ← Flask 服务
│   ├── uploads/                ← X光片
│   ├── posters/                ← 海报
│   ├── qrcodes/                ← 二维码
│   ├── spine_curved.txt        ← 弯曲脊柱坐标
│   └── spine_straight.txt      ← 直立脊柱坐标
├── assets/
│   ├── fonts/                  ← 中文字体
│   ├── backup_posters/         ← 备选海报
│   └── audio/                  ← 背景音乐
└── docs/                       ← 文档
```

---

# 附录 B：Vibe Coding 最佳实践

## 怎么描述需求才能一次到位？

```
❌ 太模糊：
  "帮我做个好看的粒子效果"

✅ 刚刚好：
  "做一个 1200 个粒子的脊柱曲线，粒子是柔和发光的圆形（不是硬边的），
   沿曲线分布但有 ±0.05 的随机偏移形成带状。颜色从蓝紫(#2d1b69)渐变到
   暖金(#d4a24c)，渐变程度由变量 blend(0~1)控制。背景是径向渐变的深蓝色。
   加上 Bloom 后处理让亮的地方发光。"

❌ 太长太碎：
  先说一点 → AI 写了 → 再说下一点 → AI 又改 → 来回 20 轮

✅ 一次性说完：
  把所有需求整理成结构化的 Prompt 一次发给 AI
  （就像本文档里给你的那些 Prompt 模板那样）
```

## 遇到报错了怎么办？

```
第1步：完整复制报错信息（不要只复制一行，要完整的 stack trace）
第2步：连同你最近的改动一起发给 AI
第3步：告诉 AI "这是我看到的报错，帮我修复"

不要自己尝试修！
  →你不是程序员，自己改可能引入新问题
  →直接交给 AI，它 30 秒就能定位并修复
```

## 效果不满意怎么迭代？

```
不要说："不好看"（AI 不知道哪里不好看）

要说：
  "现在的效果有两个问题：
   1. 粒子太密集了，看起来像一条实线而不是粒子带，希望更疏散一些
   2. 颜色过渡太突然，希望渐变更平滑
   3. Bloom 太强了，边缘过曝"

或者更好——截图：
  "截图里可以看到脊柱中间这段颜色不太对，
   我希望它是渐变的而不是突然跳变的"
   （如果你用的 AI 支持图片输入的话）
```

## 代码版本管理

```
每次 AI 给你一个能跑的版本后：
  1. 复制整个项目文件夹，命名为 backup_日期_功能名
  例：backup_0407_spine_basic
      backup_0408_color_gradient
      backup_0410_guide_animation

  2. 这样万一新的改动搞坏了，可以立刻回退

  3. 不需要学 Git。手动复制文件夹就够了。
```

---

# 附录 C：验收标准

## 核心功能验收

- [ ] 穿上背心后，浏览器/投影中能看到传感器数据变化
- [ ] 做施罗斯呼吸时，粒子脊柱从弯曲态向直立态过渡
- [ ] 停止呼吸/错误呼吸时，脊柱恢复弯曲态（慢速衰减）
- [ ] 色彩从蓝紫（弯曲）渐变到暖金（变直）
- [ ] 振动马达在正确时机提供触觉引导
- [ ] 认知引导动画流畅播放（情感共鸣+疾病介绍+呼吸教学，含节拍器+跟做练习）
- [ ] 三段式节奏正确（灵敏度×1.5 → 归1.0 → 自动叠加+0.2）

## AI 功能验收

- [ ] 上传 X 光片后 10 秒内完成坐标提取并更新粒子曲线
- [ ] 体验结束后 30 秒内生成个性化艺术图
- [ ] 二维码能正常扫码打开并保存图片

## 展览体验验收

- [ ] 完整体验流程 5 分钟内完成
- [ ] 暗场环境下视觉效果出色
- [ ] 连续运行 2 小时无崩溃
- [ ] 浏览器全屏 → HDMI 投影稳定不出问题
