# ③ 呼吸体验环节 —— 详细设计方案

> **这是整个装置的核心环节。** 用户穿着智能背心，做施罗斯呼吸，实时看到粒子脊柱从弯变直。
>
> 本文档聚焦于**体验过程中的视觉、听觉、触觉表现和节奏设计**。传感器方案、blend 计算公式、马达策略等硬件基础已在 **①待机与准备** 中定义（§5 传感器方案 + §6 马达策略），此处不再重复，仅引用。

---

## 1. 环节定位

- 时长：**固定 2 分钟**（120 秒）
- 从认知引导动画末尾无缝过渡进入
- 体验结束后自动过渡到 AI 艺术图生成环节（→ ④生成与带走）

---

## 2. 体验节奏设计

> 核心原则：让每一个参观者在 2 分钟内都能完成"从不会 → 学会 → 看到变化 → 达到高潮"的完整体验弧线。

```
0:00                0:30                1:30                2:00
 │───── 教学期 ──────│────── 自由呼吸期 ──────│──── 高潮收束 ────│
 │                   │                        │                  │
 │ 马达引导节奏       │ 马达全程同一节奏不变           │ 视觉最美状态      │
 │ blend灵敏度↑       │ 用户自主呼吸            │ 色调暖金高潮      │
 │ 快速看到效果       │ 视觉稳步提升            │ 粒子绽放/扩散     │
 │ 建立信心           │ 沉浸体验                │ 情绪顶峰          │
```

### 2.1 教学期（0:00 - 0:30）

**目标**：让用户在 30 秒内学会呼吸、看到效果、建立信心。

| 要素 | 设计 |
|------|------|
| **开篇文字（0-5s）** | 屏幕底部居中显示 `"吸气 → 凸起的一侧"`，淡入(1s)→停留(3s)→淡出(1s)。小字号(~20px @1080p)，淡色低透明度(~0.5)，无背景无边框。之后全程不再出现任何文字 |
| 马达（默认模式） | M1（左胸）+ M2（右腰）**全程保持同一节奏和强度**。每次吸气前 0.5 秒嗒嗒嗒。不减弱、不停止、不切换模式——用户习惯了就会自然忽略它。详见① §6.3 振动时序表 |
| 马达（个性化模式） | **关闭马达**——上传了 X 光片的患者弯型未知，关闭避免误导。**但音频引导音仍正常播放**（详见① §6.2 双模式策略） |
| **呼吸引导音效（新增）** | **4秒吸 → 2秒停 → 4秒呼**循环播放，统一清晰音量(~0.6)。与②教学段③-b 的节拍器同一套声音语言（无缝衔接）。详见 §5 新增小节 |
| blend 灵敏度 | **调高**——即使呼吸幅度不大，也给明显的视觉反馈。灵敏度倍率 = 1.5（正常值的 1.5 倍） |
| 视觉反馈 | 粒子脊柱明显响应，让用户立刻看到"我做对了" |
| blend 预期范围 | 教学期结束时 blend 约 0.2 ~ 0.3 |

**为什么灵敏度要调高？**
- 用户第一次做施罗斯呼吸，动作不到位很正常
- 如果费了半天劲看不到任何变化，会沮丧、放弃
- 先给正反馈，建立"我做对了"的信心，后面自然越做越好

**为什么文字只出现5秒就消失？**
- 只说最关键的一个信息："气往哪吸"。其他（节奏、停顿）交给音频和马达
- 参照正念冥想引导惯例：只有开头和结尾有语言提示，中间纯靠节奏进入沉浸状态
- 文字留在画面上会持续抢夺粒子脊柱的视觉注意力

### 2.2 自由呼吸期（0:30 - 1:30）

**目标**：用户进入自主呼吸状态，沉浸式感受脊柱变化。

| 要素 | 设计 |
|------|------|
| 马达 | **全程保持同一节奏强度，不减弱、不停止**。用户进入沉浸状态后会自然忽略它（类似戴手表——一开始有感觉，过一会儿就意识不到了） |
| 呼吸引导音效 | **统一音量持续播放**（~0.6），不渐弱。参照正念冥想引导惯例：引导音全程保持同一音量才能让用户稳定进入 trance 状态，音量变化反而会打断注意力 |
| blend 灵敏度 | 渐变回归正常值（1.0），15 秒过渡 |
| 视觉 | 粒子脊柱稳步从弯曲态向直立态过渡 |
| 色调 | 随 blend 值从蓝紫向暖金渐变 |
| blend 预期范围 | 自由期结束时 blend 约 0.5 ~ 0.7 |
| 衰减机制 | 如果用户停止正确呼吸，blend 缓慢回落（不是瞬间归零） |

**衰减机制详解**：
```
正确呼吸中：blend 上升速率 = sensor_value × sensitivity
停止/错误呼吸：blend 下降速率 = 0.02/秒（约50秒才回落到0）

设计意图：
- 上升快、下降慢 → 用户不会因为偶尔中断而"功亏一篑"
- 保持积累感——"我之前的呼吸没有白费"
- 但如果完全不做，视觉也会慢慢退回去，保持因果关系的真实感
```

### 2.3 高潮收束期（1:30 - 2:00）

**目标**：无论用户做得如何，都给一个美的、有成就感的结尾。

| 要素 | 设计 |
|------|------|
| blend 行为 | 在用户当前 blend 基础上，系统**额外叠加一个缓慢上升**，确保结尾时视觉效果达到峰值 |
| 叠加量 | 最后 30 秒内自动叠加约 +0.15（按 0.005/秒 × 30s 计算）。最终 blend 被 clamp 到 0~1 |
| 视觉效果 | 粒子亮度增强、Bloom 加大、粒子微微扩散绽放 |
| 色调 | 达到最暖的金色 |
| 音频 | 音乐渐强，到达情绪高点 |
| **呼吸引导音效** | **约 115 秒开始 fade out（~3-5 秒过渡至 0），配合收束语出现自然让位。不是"渐弱"，而是仪式性的收束——告知用户引导正在结束** |
| blend 最终范围 | 约 0.7 ~ 1.0（取决于用户表现） |

**为什么要自动叠加？**
- 保证**每个参观者**都能看到"脊柱明显变直"的效果
- 做得好的用户 → blend 可以到 0.9~1.0（几乎完全变直），很有成就感
- 做得一般的用户 → blend 也能到 0.7~0.8，视觉上有明显改善
- 不会出现"我做了2分钟啥也没看到"的尴尬

**允许完全变直（blend = 1.0）的情况**：
```
如果用户自由呼吸期结束时 blend ≥ 0.6（呼吸做得不错）
+ 高潮期系统叠加约 +0.15（0.005/秒 × 30秒）
+ 用户继续呼吸的贡献
→ blend 完全可能达到 1.0

这时候脊柱完全变直，是对用户努力的最高奖赏
不刻意限制上限——如果他真做到了，就让他看到
```

---

## 3. blend 参数在体验中的应用

> blend 的完整计算公式和数据流已在 **①待机与准备 §5** 中定义。本节仅描述 blend 在体验环节中如何驱动视觉表现。

### 3.1 前端每帧处理逻辑（JavaScript）

> 数据接收链路：Arduino USB串口 → Python(pyserial) → Flask-SocketIO → 浏览器(WebSocket) → JSON.parse → 以下处理流程

```
进入 EXPERIENCE 阶段时（按钮按下那一刻）：
    sessionSeed = Math.random()          // 每个 session 唯一，详见 §7.2

WebSocket 收到传感器数据 { s1, s2, s3, s4, b, timestamp }
    │
    ▼
① 归一化：
   rawScore = sensorData.chestDiff * weightChest + sensorData.waistDiff * weightWaist;
   normalized = rawScore / THRESHOLD;
    │
    ▼
② 灵敏度（随时间渐变）：
   sensitivity = getCurrentSensitivity(elapsedTime)
   // 教学期(0-30s): 1.5 → 自由期(30s+): 渐变为 1.0
   displayBlend = normalized * sensitivity
    │
    ▼
③ 累积模式：
   if displayBlend > 0:
       blend += displayBlend × RISE_RATE × dt
   else:
       blend -= DECAY_RATE × dt
   // 【保底】blend 绝不会静止不动，详见 §7.1：
   blend += MIN_FLOOR_RATE × dt          // 最小增速 0.002/秒，全程生效
   if elapsedTime > 90:
       blend += EXTRA_FLOOR_RATE × dt     // 高潮期额外 +0.005/秒
    │
    ▼
④ Clamp + EMA 平滑：
   blend = clamp(blend, 0, 1)
   smoothedBlend = EMA(blend, alpha=0.15)
    │
    ▼
└──→ [最终 smoothedBlend] ──→ Three.js 粒子脊柱形态
                           ──→ ShaderMaterial 色调 uniform
                           ──→ EffectComposer Bloom 强度

（粒子渲染中使用 sessionSeed 做装饰性随机输入，详见 §7.2）
```

### 3.2 关键参数（需现场调试）

| 参数 | 建议初始值 | 说明 |
|------|-----------|------|
| 上升速率 | 0.008/帧 | blend 每帧最大上升量（60fps 下约 0.48/秒） |
| 衰减速率 | 0.02/秒 | 停止呼吸后的回落速度 |
| 教学期灵敏度 | 1.5 | 前 30 秒的灵敏度倍率 |
| 灵敏度过渡时间 | 15 秒 | 从 1.5 过渡到 1.0 的时间 |
| 高潮期自动上升 | 0.005/秒 | 最后 30 秒的自动叠加速率（共约 +0.15） |
| 差值阈值 | 现场标定 | 传感器差值达到多少算"正确呼吸"（见 ① §5.3） |

---

## 4. 视觉设计

### 4.1 整体画面构成

```
┌─────────────────────────────────────────────┐
│                                             │
│        ·  · ·                               │
│      ·    ·    · ·  环境微粒                 │
│    ·         ·                              │
│                                             │
│              ╭──╮                           │
│             ╱    ╲     粒子脊柱              │
│           ╱   ✦✦  ╲    （核心主体）          │
│          │  ✦✦✦✦✦  │                        │
│          │ ✦✦✦✦✦✦  │   蓝紫→暖金渐变        │
│           ╲ ✦✦✦✦  ╱                         │
│            ╲    ╱                            │
│              ╰──╯                           │
│                                             │
│     ·       ·                               │
│          ·       ·    环境微粒               │
│     ·                                       │
│                                             │
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓ │
│  深色渐变背景（深蓝紫 → 纯黑）               │
└─────────────────────────────────────────────┘
```

### 4.2 各视觉层次

| 层级 | 内容 | 参数 | 说明 |
|------|------|------|------|
| **① 背景层** | 深色径向渐变 | 中心：深蓝紫 `#0a0a1a`，边缘：纯黑 `#000000` | 不是死黑，有微弱的深空感 |
| **② 环境微粒层** | 漂浮的微小光点 | 数量约 200-300，大小 1-3px，透明度 10%-30% | 营造空间纵深感，像星尘 |
| **③ 粒子脊柱层** | 核心主体 | 详见 4.3 | 所有视觉焦点 |
| **④ 光晕层** | 脊柱周围的柔光 | 跟随脊柱曲线的高斯模糊光带 | 让脊柱"发光"而非硬边 |
| **⑤ 后处理层** | Bloom + 轻微色差 | Bloom 强度随 blend 增大 | 越变直越"发光" |

### 4.3 粒子脊柱详细设计

**粒子形态**：柔和的发光球体（不是硬边圆点）

| 参数 | 值 | 说明 |
|------|-----|------|
| 粒子数量 | 800 ~ 1200 | 沿脊柱曲线散布 |
| 粒子大小 | 核心粒子 3-8px，边缘粒子 1-3px | 中心密大，边缘稀小 |
| 粒子材质 | Point Sprite + 高斯模糊纹理 | 柔和发光感，无硬边 |
| 分布方式 | Three.js BufferGeometry + Points 沿曲线散布，ShaderMaterial 中加 Noise 偏移 | 不是一条线，是有宽度的"粒子带" |
| 粒子带宽度 | 约曲线两侧 ±0.05（归一化） | 像一条发光的河流/脊 |
| 呼吸脉动 | 粒子大小随时间微弱呼吸（±10%） | JavaScript 正弦波驱动，周期约 3-4 秒 |
| 微动 | 每个粒子有轻微的 Noise 位移动画 | 不完全静止，有生命感 |

### 4.4 色彩系统

**色彩映射原则**：blend 值同时控制脊柱形态和色调。

```
blend = 0.0 (弯曲态)           blend = 1.0 (变直态)
┌──────────────────────────────────────────────┐
│  深蓝紫                              暖金    │
│  #2d1b69  →  #4a2d8f  →  #7b4fb5  →  #d4a24c │
│                                              │
│  沉静      过渡        明亮       温暖        │
│  内敛      释放        舒展       绽放        │
└──────────────────────────────────────────────┘
```

**颜色渐变关键帧**（Shader uniform）：

| 位置 (blend) | 颜色 | RGB | 情绪 |
|-------------|------|-----|------|
| 0.0 | 深蓝紫 | (45, 27, 105) | 沉寂、内收 |
| 0.25 | 蓝紫 | (74, 45, 143) | 开始觉醒 |
| 0.5 | 浅紫 | (123, 79, 181) | 舒展、释放 |
| 0.75 | 紫金过渡 | (180, 130, 100) | 温暖涌入 |
| 1.0 | 暖金 | (212, 162, 76) | 绽放、完成 |

**blend 同时驱动的其他视觉参数**：
- 粒子亮度 uniform（0.6 → 1.2，越变直越亮）
- UnrealBloomPass strength（0.3 → 0.8）
- 环境微粒亮度（微微随之变亮）
- 正确呼吸时环境微粒微微向脊柱方向聚拢

### 4.5 光晕与后处理

```
渲染层级：

Layer 1: 背景渐变 (CSS radial-gradient 或 ShaderMaterial 径向渐变)
    ↓ 合成
Layer 2: 环境微粒 (Three.js Points，低透明度)
    ↓ 合成
Layer 3: 粒子脊柱 (Three.js BufferGeometry + Points + ShaderMaterial，主体渲染)
    ↓ 合成
Layer 4: 脊柱光晕 (EffectComposer BlurPass 叠加回去，制造 glow 效果)
    ↓
后处理链（EffectComposer + Pass）：
    ├── UnrealBloomPass (阈值随 blend 降低 → 越变直越发光)
    ├── 轻微色差 ChromaticAberration (边缘1-2px偏移，增加质感)
    └── 轻微暗角 Vignette (边缘压暗，聚焦视觉中心)
```

---

## 5. 音频设计

### 5.1 音频结构

> **两轨音频：轨①背景音乐 + 轨②呼吸引导音效。** 参照正念冥想引导惯例：引导音全程保持同一音量才能让用户稳定进入沉浸状态。

| 时段 | 轨① 背景音乐 | 轨② 呼吸引导音效 |
|------|-------------|----------------|
| 教学期 (0-30s) | 延续引导的环境音 | **清晰循环**（~0.6）：4秒吸气→2秒停→4秒呼气。配合马达嗒嗒嗒多通道同步引导 |
| 自由期 (30-90s) | 舒缓器乐/电子氛围渐入 | **同一音量持续播放**（~0.6）。不渐弱。用户进入沉浸状态后会自然忽略它 |
| 高潮期 (90-115s) | 音乐渐强，音色变暖 | **同一音量持续播放**至 ~115 秒 |
| 收束期 (115-120s) | 音乐到达情绪高点 | **fade out 至 0**（~3-5 秒过渡）。配合收束语出现，仪式性让位 |

### 5.2 音频与呼吸的关系

```
音频不直接跟随传感器数据（会太嘈杂），而是：
├── 轨① 背景音乐：预设时间线，自动演进
├── 轨① 环境音色：色调随 blend 缓慢变化（蓝=冷色pad，金=暖色pad）
└── 轨② 呼吸引导音效：全程固定节奏、固定音量（正念引导标准做法）

轨② 呼吸引导音效详情：
├── 节奏：4秒吸气 → 2秒停顿 → 4秒呼气 = 10秒/循环
├── 与②教学段③-b节拍器同一套声音语言（无缝衔接）
├── 全程统一音量 ~0.6（参照正念冥想：引导音不变才能稳定注意力）
├── 约 115 秒开始 fade out（收束，不是"减弱"）
└── 素材选项：
    A) 预制采样：轻柔气流吸气声 + 缓慢呼气声
    B) 合成音：Web Audio API Oscillator 正弦波包络生成（无需外部素材）
    推荐先试 B，效果不够再换 A
```

### 5.3 前端音频实现（Web Audio API）

```
AudioContext（用户交互后创建，浏览器策略要求）
    │
    ├──→ 轨① Audio（加载预制背景音乐，2分钟时长）
    │     │
    │     └──→ GainNode_1 ← JavaScript 计时控制渐入渐出
    │           // gain.gain.linearRampToValueAtTime()
    │           // 音量随阶段：0.3 → 0.5 → 0.8
    │
    ├──→ 轨② 呼吸引导音效（新增）
    │     ├── 方案A：AudioBuffer 加载吸气+呼气采样
    │     │   setInterval(10000) 循环播放
    │     └── 方案B（推荐先试）：OscillatorNode 正弦波包络
    │        吸气: freq↑ + gain↑ (4s)
    │        停顿: gain 恒定低值 (2s)
    │        呼气: freq↓ + gain↓ (4s)
    │        无需外部素材，代码直接生成
    │     └──→ GainNode_2 ← 统一音量 ~0.6
    │           // 约115s: linearRampToValueAtTime(0, t+5s) 收束fade out
    │
    └──→ AudioContext.destination → 投影仪/显示器内置音箱
         或外接小音箱

轨① 音色变化方案：
    准备两轨音频：冷色调版 + 暖色调版
    用两个 GainNode + blend 值做 Crossfade
    简单但效果好
```

---

## 6. 结束过渡

### 6.1 触发

**固定时间制**：JavaScript `performance.now()` 或 `Date.now()` 计时到 120 秒后自动进入结束过渡。

不采用 "blend 达标就提前结束" 的方式——保证每人体验时间一致，也确保高潮收束期有完整表现。

### 6.2 结束过渡动画（约 18 秒）

> **采用正念冥想引导的收束结构：** 不是突然切断，而是给用户一个"正在安全地结束"的仪式性信号。
>
> 核心变更（v5）：**从 ~8秒延长到 ~18秒（120s→138s）。** 原方案8秒内塞了4条信息，"慢下来"刚说完就接下一个指令——自相矛盾。参照真实冥想引导收束节奏：**先静、再引、再别、最后谢**。

```
时间       视                                      音频              文字
─────────┬──────────────────────────────────────┬─────────────────┬──────────────
120s      │ 粒子脊柱定格                         │ 引导音效已接近   │ （纯画面，
          │ 动作完全停止，只看                    │ 归零(自115s起    │  无文字)
          │                                      │ 已fade out ~5s)  │ 给用户3秒
123s      │ 粒子开始极缓慢向四周扩散              │                 │ "慢下来……"
          │ 不是炸开，是融化                      │                 │ (fade in→停留
126s      │ 轮廓模糊，像冰化成水                  │                 │  3s→fade out)
          │                                      │ 引导音已归零     │
129s      │ 粒子继续漫散                         │ 只剩背景音乐     │ "最后一次深呼吸……"
          │ 光芒变柔                              │ 渐强中           │ (同上)
132s      │ 大部分粒子已离开原位                  │                 │
          │ 画面越来越空灵                        │                 │ "旅程结束。"
135s      │ 少数粒子飘向中央                      │                 │ "谢谢你和它一起呼吸。"
          │ 汇聚为微光                            │                 │ (fade in→停留
138s      │ 过渡到 AI 生成等待画面                │                 │  4s→fade out)
─────────┴──────────────────────────────────────┴─────────────────┴──────────────
```

**收束语设计意图**：

| 原则 | 说明 |
|------|------|
| **先静后语** | 120-123秒没有任何文字。体验刚结束，先让用户自己消化3秒看到的最终画面，再开口 |
| **每条独立空间** | 每条文案间隔3秒（含fade）。实际可见时间足够从容读完 |
| **从动作到告别到感谢** | "慢下来"（最后动作）→ "最后一次深呼吸"（确认）→ "旅程结束"（宣告）→ "谢谢"（致谢）。层层递进 |
| **用"谢谢"收尾** | 把用户从"被实验对象"变成"参与者"。正念引导惯例用感谢关闭容器 |
| **音频配合** | 文字开始前引导音就开始 fade out(~5s)。第一条文字出现时引导音已很低或消失 |
| **总时长 ~18 秒** | 比原来多10秒。这10秒是整个体验最有仪式感的部分——值得 |

**设计意图（整体）**：
- 不是突然黑屏切走，而是粒子自然"绽放→消散"
- 隐喻：呼吸的力量扩散到整个身体
- 加上文字收束后，用户得到的情绪落点是："哦，结束了。好美。谢谢。"——完整的情感闭环

### 6.3 前端实现要点

```
JavaScript 计时器（120秒结束触发）
    ├── 0-5s: 开篇文字 "吸气 → 凸起的一侧" (fade in/out)
    ├── 5-30s: 教学期参数组 + 呼吸引导音效(清晰) + 马达
    ├── 30-115s: 自由+高潮期参数组 + 呼吸引导音效(同一音量) + 马达(不变)
    ├── 115-120s: 呼吸引导音效 fade out (~5s)
    └── 120-138s: 结束过渡（~18秒正念收束，v5）
         │
         │  120-123s: 纯画面静默（无文字），粒子定格
         │  123-126s: "慢下来……" (fade in/out)
         │  129-132s: "最后一次深呼吸……" (fade in/out)
         │  135-138s: "旅程结束。/谢谢你和它一起呼吸。" (fade in/out)
         │
         ├── BufferGeometry position: 粒子从曲线分布 → 球形/随机分布（Noise振幅渐增，极缓慢）
         ├── ShaderMaterial opacity: 1.0 → 0.0
         ├── HTML/CSS 文字层: 收束语分步 fade in → fade out (4条信息, ~18秒)
         ├── 轨② GainNode_2: linearRampToValueAtTime(0, t+5s) 引导音消失
         └── SocketIO emit: 通知 Flask 服务开始生成 AI 艺术图
```

---

## 7. 异常处理与鲁棒性保障

### 7.1 blend 保底机制（核心）

> **问题**：FSR402 压力传感器对贴合度非常敏感，且大部分观众是第一次做施罗斯呼吸，很可能：
> - 呼吸方式不对（普通呼吸而非定向呼吸）
> - 动作不到位、紧张僵硬
> - 背心贴合度不够理想
>
> 这些都可能导致 blend 长时间接近 0。**不能让观众体验完觉得"啥也没发生"。**

#### 核心原则：传感器是主角，保底是安全网

```
blend 实际增速 = max(传感器驱动增速, 最小保底增速)

传感器读得好 → 传感器驱动 >> 保底 → blend 涨得快 → 终点高（0.8~1.0）
传感器读得弱/没做对 → 传感器驱动 ≈ 保底 → blend 涨得慢但也在涨 → 终点中（0.3~0.5）
完全没信号 → 纯保底撑着 → blend 最慢涨 → 终点低（0.2~0.4）

三种人看到的终点不同，传感器有意义，同时没有人会看到"完全没变化"
```

#### 保底参数

| 参数 | 值 | 说明 |
|------|-----|------|
| **最小保底速率** | 0.002 / 秒 | 全程生效，blend 绝不会静止不动 |
| **高潮期额外保底** | +0.005 / 秒 | 最后 30 秒在基础保底之上再加速 |
| **120秒纯保底的终点** | 约 **0.24 + 0.15 = 0.39** | 完全没传感器输入也能到 ~0.4 |
| **不做任何弹窗/提示** | — | 用户不知道自己"做得不好"，体验完整 |

#### 为什么不做弹窗提示？

展览场景下：
- 弹窗 = 打断沉浸感 + 让观众尴尬
- 观众不是患者，不需要精确的呼吸训练反馈
- 他们要的是一个**完整的视觉旅程**
- 设计者在场可以辅助调整背心，但不应该依赖这个

#### 对比：不同表现者的预期终点

| 表现 | blend 来源构成 | 预期最终 blend | 视觉效果 |
|------|--------------|---------------|---------|
| 做得好（学会施罗斯呼吸） | 传感器驱动为主（占 80%+） | **0.8 ~ 1.0** | 脊柱明显变直，暖金色，粒子绽放强烈 |
| 普通表现（偶尔做对） | 传感器 + 保底各半 | **0.5 ~ 0.7** | 有明显改善，颜色过渡到偏暖 |
| 没做对 / 信号微弱 | 保底为主（占 70%+） | **0.3 ~ 0.5** | 有轻微变化，颜色从深蓝紫走到中间态 |
| 完全无信号 | 纯保底 | **0.35 ~ 0.45** | 有可见变化（从弯到略直），但幅度最小 |

### 7.2 Session 随机种子

> **问题**：如果同一个用户体验两次、或者两个用户的 blend 变化曲线相同，他们看到的画面会**精确地一模一样**——粒子位置、Noise 偏移、脉动相位全部由确定性算法驱动。
>
> 这是因为当前设计中所有"随机"元素都用 `time` 做种子，同一时间点结果完全相同。

#### 解决方案：每个 session 生成唯一随机种子

```
进入 EXPERIENCE 阶段时（按钮按下那一刻）：
    sessionSeed = Math.random()        // 0~1 的随机浮点数，每个 session 不同

后续所有"随机"计算都改为用 (sessionSeed + time) 做输入：

    粒子初始位置偏移 = noise(position + sessionSeed)
    Noise 微动位移 = noise(particleId, time * speed + sessionSeed)
    粒子大小随机系数 = hash(particleId + sessionSeed) * 0.2 + 0.9   // ±10% 内随机
```

**效果**：

| 维度 | 无 seed（之前） | 有 seed（现在） |
|------|---------------|----------------|
| 同一用户两次体验 blend 曲线相近 | 画面几乎完全一样 | 大体感觉一致（弯曲度/色调），但粒子分布细节不同 |
| 两个不同用户 blend 相同 | 画面完全复制粘贴 | 结构相似但细节各异 |
| 同一次体验内的帧间连续性 | ✅ 平滑 | ✅ 平滑（seed 不变，只加 time） |

#### 种子不影响的部分

以下内容**不受 seed 影响**（也不应该受影响，它们由 blend 决定才有意义）：
- 脊柱弯曲曲线形态（blend 插值关键帧）
- 色彩映射（blend → 色相）
- Bloom 强度（blend → brightness）
- 音频推进（固定时间线）

**seed 只影响"装饰性随机"——粒子怎么散布、怎么微动、每颗多大。** 核心的"呼吸越好→越直→越暖"因果关系不变。

### 7.3 其他异常

| 异常情况 | 检测方式 | 处理 |
|---------|---------|------|
| 用户中途脱下背心 | 传感器基线值骤降 | 暂停体验，投影显示"请重新穿戴背心"；30秒无恢复则重置 |
| 传感器数据异常跳变 | 差值变化率过大 | Arduino 端滤波处理，前端 EMA 指数移动平均平滑 |
| Arduino 断连 | Python pyserial 读取超时 / WebSocket 断连 | 投影显示友好提示，你（设计者）手动排查 USB 连接 |

---

## 8. 性能预估

| 资源 | 预估负载 | 说明 |
|------|---------|------|
| 粒子数 | ~1500 (脊柱1200 + 环境300) | GPU 轻松承载 |
| Render 分辨率 | 1920×1080 | 标准投影分辨率 |
| 帧率目标 | 60fps | requestAnimationFrame 驱动，现代浏览器默认 60fps |
| Serial 通信 | 115200 baud, ~30 Hz | Arduino 每秒发 30 次数据足够 |
| 音频 | 1-2 轨 WAV | 几乎无负载 |

**浏览器性能建议**：
- 确保用**支持 WebGL 2.0** 的现代浏览器（Chrome / Edge 推荐）
- 不要开过多 EffectComposer 后处理 Pass——如果卡就先砍色差和暗角
- Bloom（UnrealBloomPass）是最重要的后处理，必须保留
- 使用 `requestAnimationFrame` 驱动渲染循环，避免 `setInterval`
- 粒子数控制在 1500 以内，使用 BufferGeometry + Points（非 Mesh）以获得最佳 GPU 性能

---

## 9. 待确认/后续

- [ ] 背景音乐风格选定（需要找参考或制作）
- [ ] 呼吸引导音效素材确定：预制采样 vs 合成音（推荐先试 Web Audio API Oscillator 方案）
- [ ] 收束语文案最终定稿（当前：慢下来→最后一次深呼吸→旅程结束→谢谢你和它一起呼吸）
- [ ] 开篇提示文字文案最终定稿（当前："吸气 → 凸起的一侧"）
- [ ] 粒子具体参数需要在浏览器中实际调试（Chrome DevTools + Three.js Inspector）
- [ ] 传感器差值阈值需要穿上背心实测标定
- [ ] 结束过渡动画的粒子扩散效果需要在浏览器中调试
