# CLAUDE.md — raina-project 开发规则

## 重要：改代码前必须得到用户明确确认

**严格执行**：任何代码修改之前，必须先说明计划，等待用户说"可以"/"go"/"写吧"等明确指令后才能动手。

- 分析问题 → 说方案 → 等确认 → 才写代码
- 如果用户没有明确说"开始写"，只说方案，不写代码

## 推送规则

每次推送代码后，必须给用户列出终端要输入的命令（git pull / 启动命令等），不要让用户自己去想。如果分支变了，也要给出 checkout 命令。

## 项目信息

- 项目：raina-project — 正念呼吸可视化（Three.js 粒子脊柱）
- 主文件：`spine-viz/app.js`
- 启动预览：`cd spine-viz && npm run dev -- --host`
- 主分支：`main`（从 main 出新分支开发）

## 架构概览

五层粒子架构：
- Layer A：骨骼柱体（20000粒子，空心管）
- Layer B：椎节椭圆（13×400=5200粒子）
- Layer C：弥散粒子流（12000粒子，水流/烟雾效果）
- Layer D：脊柱辉光线（60粒子）
- Layer E：环境星尘（300粒子）

blend 值（0=弯曲蓝紫, 1=直立金色），由 Socket.io 或调试滑块控制。
