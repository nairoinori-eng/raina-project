# 产品需求文档（PRD）

## 脊柱侧弯患者居家康复训练智能交互系统

> 基于施罗斯呼吸法 + AIGC 生成艺术反馈的沉浸式体验装置

**版本**: v1.0  
**作者**: rainayychen  
**日期**: 2026-03-27  
**Deadline**: 2026-05-10（布展评审）

---

## 1. 项目概述

### 1.1 一句话描述

一套让脊柱侧弯患者**穿上智能背心、做施罗斯呼吸、看到自己的脊柱在粒子艺术中从弯变直**的沉浸式交互装置。

### 1.2 核心价值

| 维度 | 说明 |
|------|------|
| 对患者 | 将抽象的"呼吸训练"转化为可视、可感的沉浸式体验，增强康复动力 |
| 对设计 | 交互设计 × 医疗康复 × AIGC，跨学科创新 |
| 对评审 | 现场可体验、有个性化数据、有生成艺术产出，展览效果强 |

### 1.3 设计者背景

设计者本人即脊柱侧弯患者（S型，胸椎右凸+腰椎左凸代偿），拥有自身 X 光片数据，项目兼具个人叙事与设计研究双重意义。

---

## 2. 用户与场景

### 2.1 目标用户

| 用户类型 | 画像 | 场景 |
|---------|------|------|
| **主要用户：脊柱侧弯患者** | 有 X 光片，了解基本病情，可能正在做物理治疗 | 展览现场体验个性化版本（上传自己 X 光片） |
| **次要用户：普通观众** | 无 X 光片，对脊柱侧弯不了解 | 展览现场体验共情版本（使用设计者脊柱数据） |
| **评审老师** | 关注创新性、完成度、现场效果 | 布展评审 |

### 2.2 使用场景

毕业设计展览现场。暗场空间，一人一次体验，每次约 3-5 分钟。

---

## 3. 体验流程

### 3.1 总流程

```
┌──────────┐    ┌─────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  ① 待机   │───→│ ② 引导  │───→│ ③ 体验   │───→│ ④ 生成   │───→│ ⑤ 带走   │
│ (扫码可选) │    │   动画   │    │   主体    │    │   艺术图  │    │   扫码    │
│ (手持按钮) │    │ (~1min)  │    │ (~2min)  │    │(~20-40s) │    │ (常驻展示)│
└──────────┘    └─────────┘    └──────────┘    └──────────┘    └──────────┘
```

### 3.2 各环节详细需求

---

#### ① 待机阶段（准备）

> **核心改动**：**取消 iPad，改为手持按钮 + 手机扫码上传 X 光片（可选）**。

**目标**：用户走进来看到大屏待机画面 → 可选扫码上传 X 光片 → 穿上智能背心 → 手持按钮自己按下开始体验。

| 要素 | 规格 |
|------|------|
| **展板浏览** | 入口处 A3 展板，观众自由浏览 30 秒~1 分钟 |
| **待机画面** | Three.js 散粒子场（低密度、慢漂移、冷调蓝紫灰）+ 作品名称居中（占位待定）+ 右下角上传 QR 码（含提示文字）。极低音量环境背景音。详细设计见 **①§2.5** |
| **X 光片上传（可选）** | 手机扫投影右下角 QR 码 → 打开上传页面 → 拍照/选图 → 上传成功后手机提示「✓ 已上传，请按开始按钮」；不传也行，直接用默认脊柱数据体验。通过公网隧道（ngrok/Cloudflare Tunnel）暴露服务，手机用 4G/5G 即可，不需要连 WiFi |
| **背心穿戴** | 设计者在现场协助调整松紧和传感器位置，约 30-60 秒 |
| **传感器** | FSR402 压力传感器 × **4**（胸廓左右 ×2 + 腰部左右 ×2）+ 振动马达 × 2，贴在背心内侧对应位置 |
| **启动方式** | 用户**自己**手持按钮按下即可启动。穿好背心 → 找个舒服的位置站好 → 手里按一下 → 引导动画开播 + 系统同时开始采集静息基线。按钮按下后：名称/QR码淡出(~300ms)→ IDLE散粒子向脊柱凝聚 → 引导动画正式开始（过渡算在 60s 内）。详见 **①§2.5 + ②§5.6** |
| **个性化入口** | 如果用户上传了 X 光片 → AI 自动提取脊柱坐标 → 引导动画中的脊柱曲线即为用户个人的；分析结果（弯曲类型、Cobb 角）在投影大屏的引导阶段展示，不在手机上展示 |

---

#### ② 认知引导（约1分钟）

> **此时用户已穿好背心站在投影前。背心传感器正在采集基线数据。**

**目标**：三件事——情感共鸣、疾病介绍、呼吸教学。总时长约 60 秒，之后无缝过渡到呼吸体验。

**实现方式**：投影引导动画（纯体验区内）。展板/展示区为独立并行流程。

| 要素 | 规格 |
|------|------|
| **投影引导动画** | Three.js 实时渲染 + HTML/CSS 叠加层，约 60 秒 |
| 内容结构 | ① 情感共鸣（12s："这是我的脊柱"）→ ② 疾病介绍+个人数据展示（10s：可视化对比+Cobb角）→ ③ 呼吸教学（33s：定位凸起侧→演示正确呼吸→跟做练习×2次）→ ④ "开始"过渡（5s） |
| **文字语气** | 情感段诗意感性，教学段指令清晰直白 |
| 详细设计 | 见 **设计方案_②认知引导.md** |

**交互**：
- 用户已穿好背心，站在投影前，**自己按下手持按钮**后自动播放
- 引导期间，传感器在后台采集基线数据
- 教学段包含呼吸节拍器 + 跟做练习（两次完整呼吸循环）
- 引导结束无缝过渡到体验主体（不切换场景）
- 引导结束后**无缝过渡**到体验主体（同一套 Three.js 渲染系统，不切换场景）

---

#### ③ 体验主体（约2分钟）

**目标**：用户做施罗斯呼吸，实时看到粒子脊柱从弯变直。

**核心视觉**：

| 要素 | 规格 |
|------|------|
| 视觉风格 | 抽象粒子风格，竖向粒子山脉/脊柱 |
| 核心隐喻 | 山脊线竖过来 = 脊柱曲线 |
| 色彩 | 蓝紫色调（弯曲/初始）→ 暖金色调（变直/目标），渐变过渡 |
| **不要** | 不要写实、不要解剖细节、不要瘆人 |
| 动态 | 粒子沿脊柱曲线分布，带微弱浮动/呼吸感 |
| 核心交互 | 用户已穿好背心，做施罗斯呼吸 → 传感器数据 → 粒子脊柱从弯曲态逐渐 blend 到直立态（引导结束无缝进入） |
| **呼吸引导** | **三通道同步：(1) 开篇 0-5s 底部文字 "吸气 → 凸起的一侧" 后消失；(2) 全程音频呼吸引导音效（轨②：4s吸+2s停+4s呼循环，统一音量~0.6，收束前 fade out）；(3) 马达全程同一节奏不变。参照正念冥想引导惯例，引导音不渐弱。详见 **③§2 + §5 + §6* |

**技术原理**：

```
传感器数据 → Arduino USB串口(pyserial) → Flask-SocketIO → 浏览器(Three.js)
→ 服务端映射 (0~1) → SocketIO 推送 blend 参数

弯曲曲线 (JSON坐标) ─┐
                       ├→ Three.js 粒子插值(lerp) → Points(ShaderMaterial) → 渲染
直立曲线 (JSON坐标) ─┘
```

- `blend = 0`：完全弯曲（初始状态，蓝紫色调）
- `blend = 1`：完全变直（目标状态，暖金色调）
- 传感器数据驱动 blend 值，呼吸越到位，脊柱越直

**触觉反馈**：
- 振动马达**全程保持同一节奏和强度**（不减弱、不停止）
- 引导用户将气息导向正确方向（凸侧）

**音频**（双轨）：
- **轨① 背景音乐**：舒缓空灵，随 blend 从冷色调渐变到暖色调
- **轨② 呼吸引导音效（新增）**：全程固定节奏（4s吸+2s停+4s呼循环），统一清晰音量。约 115 秒开始 fade out 收束
- 由 Web Audio API 播放，不需额外音响设备

---

#### ④ AI 生成个性化艺术图（约30秒）

**目标**：体验结束后，AI 根据用户的呼吸数据生成一张独一无二的艺术图。

| 要素 | 规格 |
|------|------|
| 触发时机 | 体验主体**计时到 120 秒后自动触发**（固定时间制，不采用"blend 达标即提前结束"方式。详见 ③§6.1） |
| AI 模型 | Gemini API 文生图功能（基于 Gemini 2.0 Flash / Pro） |
| 输入数据 | 用户的呼吸曲线数据 + 脊柱弯曲程度 + 体验时长等参数 |
| 输出 | 一张个性化的生成艺术图，风格与主视觉一致（抽象粒子/山脉风格） |
| 每人不同 | 不同的呼吸模式 → 不同的参数 → 不同的图 |
| 展示方式 | 生成后在投影上展示约 10 秒，配以结束语 |
| 结束语示例 | **正念收束式（v5，~18秒）**：先静3秒（纯画面）→"慢下来……"→"最后一次深呼吸……"→"旅程结束。/谢谢你和它一起呼吸。"（详见③§6.2） |

---

#### ⑤ 扫码带走（常驻展示，用户扫码后按按钮结束）

**目标**：用户扫码保存自己的专属艺术图，完成体验闭环。

| 要素 | 规格 |
|------|------|
| 二维码生成 | Python 脚本生成包含图片链接的二维码，在投影上与艺术图一同展示（与待机时的上传 QR 码不同，每轮动态刷新） |
| 图片存储 | 本地 Python HTTP 服务，图片存在展览电脑上 |
| 用户操作 | 手机扫码 → 打开网页 → 长按保存图片 |
| 技术栈 | Flask/FastAPI + 二维码生成库（qrcode） |
| 网络 | 通过公网隧道（ngrok/Cloudflare Tunnel）暴露服务，手机用 4G/5G 即可，不需要连 WiFi 或局域网 |
| 降级方案 | 如果网络不可用，仅投影展示，不提供扫码 |

---

## 4. 系统架构

### 4.1 整体架构

```
┌────────────────────────────────────────────────────────────────┐
│                        展览现场                                 │
│                                                                │
│  ┌──────────┐   ┌────────────┐   ┌─────────────────────────┐  │
│  │ 智能背心  │   │ 手持按钮    │   │ 主控电脑                 │  │
│  │          │   │ (用户自按)  │   │                         │  │
│  │ 传感器 ×4 │   │            │   │  ┌─────────────────┐   │  │
│  │ 马达   ×2 │   └──────┬─────┘   │  │ Three.js 前端    │   │  │
│  │ Arduino  │          │         │  │ (WebGL实时渲染)  │   │  │
│  └────┬─────┘          │         │  └────────┬────────┘   │  │
│       │ USB Serial     │         │           │ 浏览器输出   │  │
│       │                │         │  ┌────────┴────────┐   │  │
│       ▼                ▼         │  │ Flask-SocketIO  │   │  │
│  ┌──────────────────┐   │         │  │ - pyserial读取   │   │  │
│  │  Arduino → Flask 通信     │   │  │ - SocketIO推送  │   │  │
│  │  USB串口 + pyserial      │   │  │ - API 调用       │   │  │
│  └──────────────────┘   │         │  │ - 文件读写       │   │  │
│                          │         │  │ - 二维码生成     │   │  │
│                          │         │  │ - 公网隧道       │   │  │
│                          │         │  │ - HTTP 服务      │   │  │
│                          │         │  └─────────────────┘   │  │
│                          │         └─────────────────────────┘  │
│                                                                │
│  ┌───────────────┐                      ┌──────────────────┐  │
│  │ 投影仪/显示器  │◄─────视频信号────────│  主控电脑         │  │
│  │ (主视觉输出)   │                      │                  │  │
│  └───────────────┘                      └──────────────────┘  │
│                                                                │
│  ┌───────────────┐                                             │
│  │ 展板/打卡区    │  ← 独立并行流程（展示区，非体验区内）         │
│  │ （内容待定）   │  详见后续独立文档                             │
│  └───────────────┘                                             │
└────────────────────────────────────────────────────────────────┘

                    ┌──────────────────┐
                    │  外部 API        │
                    │  Gemini API      │
                    │  (需要网络)       │
                    │  ① 脊柱坐标提取   │
                    │  ② 艺术图生成     │
                    └──────────────────┘

手机用户（公网隧道 / 4G/5G）→ Flask 上传页面 → X光片上传 + 海报扫码
```

### 4.2 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| **实时渲染** | Three.js (WebGL) | 粒子系统(ShaderMaterial/Points)、视觉效果(EffectComposer/Bloom)、浏览器全屏输出 |
| **硬件通信** | Arduino + pyserial + Flask-SocketIO | 读传感器、驱马达、数据推送至前端 |
| **后端服务** | Python (Flask + Flask-SocketIO) | 串口通信、SocketIO 推送、API 调用、文件管理、HTTP 服务、二维码 |
| **传感器** | FSR402 压力传感器 × 4（胸廓左右 + 腰部左右） | 检测胸廓/腰部呼吸膨胀差值 |
| **反馈** | 振动马达 | 触觉引导呼吸方向 |
| **结构** | 战术背心改造 | 传感器和马达的载体 |

### 4.3 数据流

```
[呼吸] → 传感器 → Arduino → USB串口 → Flask → SocketIO → Three.js粒子脊柱 → 浏览器全屏/投影
                                          ↓
                                    呼吸数据记录
                                          ↓
                                    Gemini 生成艺术图
                                          ↓
                                    二维码 → 用户手机
```

---

## 5. AI 模块需求

### 5.1 模块一：X 光片脊柱坐标提取

> 详细技术方案见 `技术方案_AI脊柱坐标提取.md`

| 项目 | 说明 |
|------|------|
| 输入 | 脊柱正位 X 光片（JPG/PNG） |
| 输出 | 17-19 个椎体中心归一化坐标（JSON），SocketIO 事件直接推送 JSON 给前端 |
| 模型 | Gemini 2.0 Flash |
| 延迟 | 约 3-5 秒 |
| 验证状态 | ✅ **已验证通过**（使用设计者真实 X 光片） |
| 备选方案 | 网络不可用时回退到默认脊柱数据 |

### 5.2 模块二：个性化艺术图生成

| 项目 | 说明 |
|------|------|
| 输入 | 呼吸数据摘要（最大值、平均值、变化曲线特征）+ 脊柱弯曲参数 |
| 输出 | 一张 1024×1024 的生成艺术图 |
| 模型 | Gemini API 文生图功能（基于 Gemini 2.0 Flash / Pro） |
| 风格 | 与主视觉一致的抽象粒子/山脉风格，蓝紫到暖金色调 |
| Prompt 策略 | 将呼吸数据参数化后嵌入 prompt，确保每人不同 |
| 延迟 | 约 10-20 秒（生成期间展示过渡动画） |
| 验证状态 | ⏳ 待验证 |

### 5.3 模块三：扫码分享服务

| 项目 | 说明 |
|------|------|
| 功能 | 将生成的艺术图通过二维码分享给用户 |
| 技术 | Python Flask + qrcode 库 |
| 网络 | **公网隧道**（ngrok / Cloudflare Tunnel），手机用 4G/5G 即可，不需要 WiFi 或局域网 |
| 流程 | 图片保存到本地 → 生成包含公网 URL 的二维码 → 投影展示 → 用户扫码访问 → 长按保存 |
| QR码策略 | 待机时的上传 QR 码和结束时的海报 QR 码是两个不同的码；每轮体验结束后自动重置，QR 码刷新（新 session_id） |
| 验证状态 | ⏳ 待开发 |

---

## 6. 硬件需求

### 6.1 采购清单

| 物品 | 数量 | 预估价格 | 备注 |
|------|------|---------|------|
| 战术背心（基础款） | 1 | ¥50-80 | 网购，选可调节尺寸的 |
| FSR402 压力传感器 | **4 片** | ¥40-80 | 胸廓左右 ×2 + 腰部左右 ×2 |
| 振动马达（coin type） | 2-4 个 | ¥20-40 | 触觉反馈 |
| Arduino Uno/Nano | 1 | ¥30-50 | 主控板 |
| 面包板 + 杜邦线 | 1套 | ¥20 | 原型连接 |
| 电阻、导线等耗材 | 若干 | ¥20 | — |
| USB 延长线 | 1 | ¥15 | Arduino 连接主控电脑 |
| **合计** | — | **¥215-345** | 预算 ¥2000 内充裕 |

### 6.2 需要借用/自备

| 物品 | 来源 |
|------|------|
| 投影仪 | 学校展厅提供 / 借用 |
| 电脑（运行 Flask + 浏览器） | 自备笔记本或台式机，需支持 WebGL |
| 手持按钮（启动体验） | USB 即插即用的实体按钮（如 Arcade 按钮模块），参观者穿好背心后自己按下启动 |
| 公网隧道服务 | ngrok 或 Cloudflare Tunnel（免费），让手机通过 4G/5G 访问上传页面和海报页面 |

---

## 7. 空间设计

### 7.1 环境

| 要素 | 规格 |
|------|------|
| 基调 | **混合式暗场**——主体暗场 + 微弱引导光线 |
| 主体 | 暗场环境，突出投影视觉效果 |
| 引导光 | 地面微弱导光条 / 入口处柔和氛围灯，引导用户动线 |
| 展板照明 | 展板上方小射灯，仅照亮展板区域 |

### 7.2 空间布局

```
┌─────────────────────────────────┐
│            展览空间              │
│                                 │
│  ┌─────┐                       │
│  │展板  │  ← 入口处              │
│  │(A3) │                        │
│  └─────┘                        │
│        ↓ 用户动线                │
│  ┌─────────────────────┐       │
│  │                     │       │
│  │    体验站位           │       │
│  │ (穿背心+手持按钮)    │       │
│  │                     │       │
│  └─────────────────────┘       │
│        ↑ 面向                   │
│  ╔═════════════════════════╗   │
│  ║                         ║   │
│  ║    投影屏幕 / 大屏       ║   │
│  ║    (主视觉输出)          ║   │
│  ║                         ║   │
│  ╚═════════════════════════╝   │
│                                 │
│  ┌──────────┐  ← 隐藏           │
│  │ 主控电脑  │     在屏幕后方     │
│  │ Arduino  │                   │
│  └──────────┘                   │
└─────────────────────────────────┘
```

---

## 8. 开发排期

> 总工期约 44 天（2026-03-27 → 2026-05-10）

### Phase 1：硬件原型（第1-2周，3/27 - 4/09）

| 任务 | 天数 | 产出 |
|------|------|------|
| 采购硬件 | 3天 | 所有元器件到货 |
| Arduino + 传感器联调 | 4天 | 传感器能稳定读数 |
| Arduino + 马达联调 | 2天 | 马达能按指令振动 |
| 背心改造 + 传感器固定 | 3天 | 可穿戴原型 |
| Arduino → Flask 串口通信 | 2天 | Flask 能读到传感器数据并通过 SocketIO 推送 |

### Phase 2：Three.js 核心视觉（第2-3周，4/07 - 4/18）

| 任务 | 天数 | 产出 |
|------|------|------|
| Three.js 粒子脊柱基础（Vibe Coding） | 3天 | Three.js 坐标→Points(ShaderMaterial) 粒子渲染 |
| 弯曲/直立 Blend 交互 | 2天 | blend 参数控制脊柱形态 |
| 颜色渐变 + 视觉打磨（EffectComposer/Bloom） | 3天 | 蓝紫→暖金色调，Bloom 等后处理效果 |
| 传感器数据驱动 blend | 2天 | 真实呼吸 → 粒子脊柱变化 |
| 触觉反馈联调 | 2天 | 马达配合呼吸节奏引导 |

### Phase 3：AI 模块 + 认知引导（第3-4周，4/14 - 4/25）

| 任务 | 天数 | 产出 |
|------|------|------|
| X 光片上传 + 坐标提取集成 | 3天 | 手机扫码上传页面 → API → 前端自动更新（SocketIO） |
| AI 艺术图生成模块 | 3天 | 呼吸数据 → prompt → 生成图 |
| 扫码分享功能 | 2天 | 二维码 + 公网隧道图片分享 + 动态QR码（每轮刷新） |
| 认知引导动画（前端CSS+JS / Vibe Coding） | 3天 | 浏览器引导动画完成 |
| 展板设计 + 印刷 | 2天 | A3 展板成品 |

### Phase 4：联调 + 打磨（第4-5周，4/25 - 5/05）

| 任务 | 天数 | 产出 |
|------|------|------|
| 全流程联调 | 4天 | 从头到尾跑通完整体验 |
| 视觉/音频细节打磨 | 3天 | 最终效果调优 |
| 稳定性测试 + Bug 修复 | 3天 | 连续运行无崩溃 |

### Phase 5：布展（5/05 - 5/10）

| 任务 | 天数 | 产出 |
|------|------|------|
| 现场布置 | 2天 | 空间、设备、投影就位 |
| 现场调试 | 2天 | 在实际环境中微调参数 |
| 预留 buffer | 1天 | 应对突发情况 |

---

## 9. 风险管理

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| Arduino 传感器不稳定 | 中 | 高 | 多买备用传感器；代码加滤波/平滑 |
| 展览现场无外网 | 高 | 中 | AI 功能降级：预存默认数据 + 预生成若干艺术图；上传和扫码功能不可用（仅投影展示） |
| 前端性能不够流畅（粒子过多/后处理太重） | 中 | 高 | 减少粒子数量；降低后处理复杂度；确保浏览器 GPU 加速开启；使用 requestAnimationFrame 优化渲染循环；考虑降级到 Canvas2D |
| 背心穿戴体验差 | 中 | 中 | 多次试穿迭代，用魔术贴快速调节 |
| Gemini API 调用限额/费用 | 低 | 中 | Flash 模型成本低；展览量有限不会超限 |
| 进度延误 | 中 | 高 | Phase 4 有 buffer；最坏情况砍扫码功能 |

### 9.1 最小可行版本（MVP / 保底方案）

如果时间来不及，以下功能可以砍掉而不影响核心体验：

| 优先级 | 功能 | 可砍？ |
|--------|------|--------|
| P0 必须 | 粒子脊柱 + 呼吸驱动 blend | ❌ 不可砍 |
| P0 必须 | 传感器背心 + 触觉反馈 | ❌ 不可砍 |
| P1 重要 | 认知引导动画 | ⚠️ 可简化为静态文字 |
| P1 重要 | AI 艺术图生成 | ⚠️ 可用预生成图替代 |
| P2 加分 | X 光片个性化上传 | ✅ 可砍，只用默认数据 |
| P2 加分 | 扫码带走 | ✅ 可砍 |

---

## 10. 验收标准

### 10.1 核心功能验收

- [ ] 穿上背心后，传感器能稳定读取呼吸数据
- [ ] 做施罗斯呼吸时，投影中的粒子脊柱从弯曲态向直立态过渡
- [ ] 停止呼吸/错误呼吸时，脊柱恢复弯曲态
- [ ] 色彩从蓝紫（弯曲）渐变到暖金（变直）
- [ ] 振动马达在正确时机提供触觉引导

### 10.2 AI 功能验收

- [ ] 上传 X 光片后 10 秒内完成坐标提取并更新粒子曲线
- [ ] 体验结束后 30 秒内生成个性化艺术图
- [ ] 二维码能正常扫码打开并保存图片

### 10.3 展览体验验收

- [ ] 完整体验流程 5 分钟内完成
- [ ] 暗场环境下视觉效果出色
- [ ] 连续运行 2 小时无崩溃
- [ ] 非专业用户能在无人指导下完成体验（认知引导有效）

---

## 11. 关联文档

| 文档 | 说明 |
|------|------|
| `完整技术实施方案.md` | **核心参考**——面向技术小白的逐步操作指南（含AI坐标提取方案） |
| `设计方案_①待机与准备.md` | 待机与准备阶段：手机上传X光片、手持按钮、传感器方案（4片）、blend计算、马达策略、背心硬件设计 |
| `设计方案_②认知引导.md` | 认知引导环节详细设计方案（投影引导动画脚本：情感共鸣+疾病介绍+呼吸教学，含技术实现） |
| `设计方案_③呼吸体验.md` | 呼吸体验环节详细设计方案（三段式节奏、视觉/音频/触觉表现） |
| `设计方案_④生成与带走.md` | AI生成艺术图 + 扫码带走环节（海报生成、QR码常驻+按钮重置、公网隧道架构） |
