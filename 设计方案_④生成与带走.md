# ④ AI 生图与扫码带走环节 —— 详细设计方案

> 本文档包含两个连续的子环节：
> - **4A. AI 生成个性化艺术图**（过渡动画 + 生成等待）
> - **4B. 扫码带走**（海报展示 + QR 码常驻 + 按钮重置）
>
> 这是体验的收尾环节。全程无人值守。

---

# 4A. AI 生成个性化艺术图

## A1. 环节定位

体验结束后，AI 根据用户的呼吸数据生成一张独一无二的 **1:1 方形艺术海报**，用于扫码保存到手机。

- 时长：**约 20-40 秒**（收束过渡~18s + AI生图~10-20s，两者有重叠；如果AI慢则额外等待。详见 A5 时间线）
- 从体验主体的粒子消融过渡动画自然衔接
- 生成完成后进入扫码带走环节

---

## A2. 数据流

```
体验主体结束
    │
    ▼
前端(Three.js)收集体验数据 → 通过 SocketIO 发送给 Flask
    │
    ▼
Flask 构造 Prompt（数据参数化嵌入）
    │
    ▼
调用 Gemini API 生成图片
    │
    ▼
返回 1024×1024 图片
    │
    ▼
Python 后处理（叠加海报排版元素）
    │
    ▼
保存到本地 → 生成二维码 → 通知前端显示
```

---

## A3. 输入数据：本次体验的"指纹"

每次体验结束时，前端向 Flask POST 以下参数，作为 AI 生图 prompt 的变量：

| 数据 | 来源 | 范围 | 用途 |
|------|------|------|------|
| `blend_final` | 体验结束时的 blend 值 | 0.0 ~ 1.0 | 反映呼吸效果，影响图的色调偏暖程度。**注意：由于 §7.1 blend 保底机制的存在，即使传感器信号微弱，blend_final 也不会低于 ~0.35（纯保底约 0.39）。做得好的用户可达 0.8~1.0** |
| `blend_max` | 体验中 blend 最高值 | 0.0 ~ 1.0 | 反映峰值表现 |
| `breath_count` | 有效呼吸次数 | 约 8~15 次 | 影响图中元素密度/层次 |
| `avg_intensity` | 平均呼吸强度 | 0.0 ~ 1.0 | 影响图的对比度/能量感 |
| `is_personalized` | 是否用了个人 X 光片 | bool | 影响 prompt 中的叙事元素 |
| `curve_amplitude` | 脊柱弯曲幅度 | 0.0 ~ 0.5 | 影响图中曲线元素的弧度 |
| `timestamp` | 体验时间 | ISO 格式 | 海报上的日期信息 |

---

## A4. AI 生图方案

### A4.1 模型

**Gemini API 文生图功能**
- 支持文生图
- 输出 1024×1024
- 延迟约 10-20 秒

### A4.2 Prompt 策略

**核心思路**：将呼吸数据参数化嵌入 prompt，确保每人生成的图都不同，但都属于同一美学家族。

**Prompt 模板**：

```
[风格基底]
Abstract generative art, dark background, particle and light flow aesthetic.
Ethereal, meditative, organic forms suggesting a vertical spine-like structure.
Minimalist composition, centered.

[色调参数 ← blend_final]
if blend_final < 0.3:
    "Deep blue-violet palette, cool and introspective mood."
elif blend_final < 0.6:
    "Blue-purple to soft lavender palette, a sense of awakening."
elif blend_final < 0.8:
    "Purple transitioning to warm amber tones, feeling of release."
else:
    "Warm golden palette with hints of deep purple, radiant and fulfilled."

[能量参数 ← avg_intensity]
if avg_intensity < 0.3:
    "Gentle, soft flowing particles, delicate and quiet."
elif avg_intensity < 0.6:
    "Moderate flowing energy, balanced rhythm of light and shadow."
else:
    "Dynamic, vibrant particle flows, strong sense of life force."

[密度参数 ← breath_count]
if breath_count < 10:
    "Sparse, meditative composition with few focal points."
elif breath_count < 13:
    "Layered composition with multiple gentle focal points."
else:
    "Rich, dense composition with many interwoven light streams."

[曲线参数 ← curve_amplitude]
"A subtle curved line {弧度描述} runs vertically through the center,
 like a river of light, suggesting transformation and alignment."

[固定约束]
"Square format 1:1. No text, no letters, no words, no human figures.
 Pure abstract art. High quality, suitable for print."
```

### A4.3 差异化保证

不同用户的数据组合 → 不同的 prompt 片段 → 不同的图。即使两个人数据相近，AI 本身的随机性也会保证每张图独一无二。

| 用户A（做得很好） | 用户B（一般） |
|-------------------|-------------|
| blend_final=0.85 → 暖金色调 | blend_final=0.45 → 蓝紫色调 |
| avg_intensity=0.7 → 动感粒子 | avg_intensity=0.3 → 柔和粒子 |
| breath_count=14 → 丰富层次 | breath_count=9 → 稀疏静谧 |
| → 金色、丰富、充满能量的图 | → 蓝紫、安静、冥想感的图 |

**两张图都很美，没有"好坏"之分**——只是风格不同，反映各自的呼吸旅程。

### A4.4 备选/降级方案

| 场景 | 处理 |
|------|------|
| API 正常 | 实时生成个性化艺术图 |
| API 超时（>30秒） | 继续等待，过渡动画多播一会儿 |
| API 失败/无网络 | 从预生成的图库中根据 blend_final 选一张最接近的 |
| 预生成图库 | 展前准备 10 张不同色调的图，按 blend 范围分段对应 |

---

## A5. 过渡动画（生成等待期）

### A5.1 设计

从体验主体的粒子消融自然衔接。用户不会感觉到"在等 AI 生图"，而是感觉"体验还在继续"。

```
时间    视觉
─────┬──────────────────────────────────────────
 0s  │ 体验主体结束，粒子从脊柱扩散消融（接上一环节）
     │
 3s  │ 散开的粒子开始向画面中央缓慢汇聚
     │ 形成一个柔和的发光团
     │ 色调保持当前 blend 对应的颜色
     │
 6s  │ 光团缓慢旋转/脉动，像一颗正在凝结的星
     │ 底部文字淡入："你的呼吸正在凝结为一件作品…"
     │
10s  │ (如果 AI 已返回) 光团"绽开" → 过渡到生成的艺术图
     │ (如果未返回) 光团继续脉动等待
     │
~15s │ AI 图到达 → 光团绽开过渡
     │
20s  │ 最终海报完整展示（含排版元素）
     │ 同时生成二维码
     │ → 进入扫码环节（4B）
─────┴──────────────────────────────────────────
```

### A5.2 "光团绽开"过渡效果

```
光团脉动中…
    │
    ▼ AI 图到达
状态机切换：前端粒子渲染 → AI 生成图展示
    但不是硬切，而是：
    │
    ├── AI 图从中心开始展开（Circle Mask，半径从0扩大到全画面）
    ├── 同时光团的粒子向四周飞散消失
    └── 过渡约 2 秒，平滑自然
```

### A5.3 前端实现要点

```
等待期（Three.js 粒子系统渲染）：
  状态机：currentMode = 'particle'
      │
      └── ParticleSystem（汇聚光团粒子）
              ├── 向中心吸引力（force = center - position）
              ├── 噪声脉动（Simplex noise 驱动位移）
              └── WebGLRenderer → 渲染到 canvas

AI 图到达后（SocketIO 'poster_ready' 事件）：
  // 加载 AI 生成图
  // 创建带遮罩的全屏平面（Fragment Shader 实现 Circle Mask）
  // uMaskRadius: 0→1（展开动画），uParticleAlpha: 1→0（粒子消失）
  // 过渡约 2秒 → 显示完整海报
```

---

## A6. 海报后处理

AI 生成的是纯艺术图（1024×1024），需要 Python 叠加排版元素后才是最终海报。

### A6.1 海报构成

```
┌──────────────────────────────────┐
│                                  │
│                                  │
│        AI 生成的艺术图            │
│       （主体，占据大部分画面）      │
│                                  │
│                                  │
│                                  │
│──────────────────────────────────│
│  [作品名称]     [日期]           │
│  [一句话文案]                    │
│                                  │
└──────────────────────────────────┘
```

> 具体排版设计后续细化。

### A6.2 Python 后处理流程

```python
# 伪代码
def create_poster(ai_image_path, metadata):
    # 1. 加载 AI 生成图
    img = Image.open(ai_image_path)  # 1024×1024
    
    # 2. 叠加排版元素（文字、日期、作品名等）
    draw = ImageDraw.Draw(img)
    # ... 排版逻辑（后续细化）
    
    # 3. 保存最终海报
    poster_path = f"posters/{timestamp}.png"
    img.save(poster_path)
    
    return poster_path
```

---

# 4B. 扫码带走

## B1. 环节定位

> **此时 AI 已根据用户的呼吸数据生成完毕专属艺术海报。**

投影展示生成的艺术海报 + 海报 QR 码，用户用手机扫码保存图片。

- 时长：**不设限时（常驻展示）**
- 用户扫码保存完成后，**按下手持按钮**触发系统重置，回到 IDLE 待机态等待下一位
- 全程无人值守

---

## B2. 用户动线

```
❶ AI 生图完成
    ↓
❷ 投影展示艺术海报 + 海报 QR 码（常驻，不自动倒计时）
    │
    ├── ❸ 用户手机扫海报 QR 码
    │     → 打开网页 → 显示高清海报图片
    │     → 长按保存到手机相册
    │
    └── ❹ 用户按下手持按钮
          → 系统重置（销毁 session、刷新 QR 码）
          → 回到 IDLE 待机画面
          → 等待下一位
```

---

## B3. 投影画面设计

### B3.1 画面构成

```
┌───────────────────────────────────────────────────┐
│                                                   │
│                                                   │
│        ┌───────────────────────┐                  │
│        │                       │                  │
│        │   🎨 AI 生成艺术海报   │                  │
│        │                       │                  │
│        │  （粒子脊柱艺术化图像） │                  │
│        │                       │                  │
│        │  基于你的呼吸数据生成   │                  │
│        └───────────────────────┘                  │
│                                                   │
│                                                   │
│              ┌─────────┐                          │
│              │  海报QR码 │  ← 和上传码不同         │
│              └─────────┘                          │
│         扫码保存你的专属艺术图                      │
│                                                   │
│                                                   │
│            [ 体验结束后请按按钮 ]                   │
│                                                   │
└───────────────────────────────────────────────────┘
```

### B3.2 双 QR 码设计

本装置在流程中有**两个不同的 QR 码**，功能完全独立：

| | 上传码（IDLE 阶段） | 海报码（SHOWING 阶段） |
|--|-------------------|---------------------|
| **出现时机** | 待机阶段常驻 | 扫码带走阶段常驻 |
| **用途** | 上传 X 光片 | 查看/保存 AI 艺术海报 |
| **链接目标** | Flask `/upload` 页面 | Flask `/poster/<session_id>` 页面 |
| **每轮是否刷新** | ✅ 是（新 session = 新链接） | ✅ 是（新 session = 新海报） |
| **过期方式** | 进入 **GUIDE 阶段后失效**（用户按下按钮、引导开始时锁定上传链接，防止体验中途上传干扰数据） | session 重置后失效 |

> 两个码不会同时出现——进入 GUIDE 后上传码消失，进入 SHOWING 后海报码出现。

---

## B4. 手机端页面

### B4.1 海报页面

```
┌──────────────────────┐
│                      │
│     🎨 你的专属呼吸艺术  │
│                      │
│  ┌──────────────────────┐   │
│  │                      │   │
│  │  [AI 生成的高清        │   │
│  │   艺术海报图片]        │   │
│  │                      │   │
│  └──────────────────────┘   │
│                      │
│    长按图片保存到相册      │
│                      │
│  仅用于本次体验           │
│  图片 24 小时后自动删除    │
│                      │
└──────────────────────┘
```

### B4.2 技术实现

| 项目 | 方案 |
|------|------|
| **访问方式** | 手机扫描投影上的海报 QR 码 → 打开公网 URL（ngrok/Cloudflare Tunnel）→ Flask 提供 poster 页面 |
| **图片加载** | 服务端直接返回已生成的 PNG 图片（`Content-Type: image/png`），不依赖 JavaScript 渲染 |
| **长按保存** | 手机浏览器原生行为——iOS 长按 → "存储图像"，Android 长按 → "下载图片" |
| **图片清理** | Flask 定时任务清理 24 小时前的旧海报文件，避免磁盘占满 |

### B4.3 边界情况

| 情况 | 处理方式 |
|------|---------|
| 扫码后打不开？（网络问题 / 隧道断了） | 显示错误提示；用户可以等一会儿再试（只要 session 还没重置） |
| 多个人同时扫同一个码？ | 同一个 session_id 对应同一张海报，都能看到，不影响 |
| 一轮结束后有人扫旧海报码？ | session_id 已失效，Flask 返回"该会话已结束" |
| 用户不想扫码？ | 直接按按钮结束即可，扫码是可选操作 |

---

## B5. 重置机制

### B5.1 触发方式

**用户主动触发**——在 SHOWING 阶段按下手持按钮（手持按钮详见 **①待机与准备 §3**）。

| 触发条件 | 行为 |
|---------|------|
| 用户按下按钮 | Flask 收到信号 → 销毁当前 session → 生成新 session → 刷新上传码和海报码 → 回到 IDLE 待机画面 |
| 不按按钮 | 海报和 QR 码**一直显示**，不限时；管理员也可以在电脑端手动操作重置 |

### B5.2 重置流程

```
用户按下按钮
    ↓
Flask 收到 RESET 信号
    ↓
① 销毁当前 session（session 数据清除、关联的上传文件标记为可清理）
    ↓
② 生成全新 session（新 session_id、新随机 token）
    ↓
③ 刷新 QR 码：
    ├── 上传码 → 新 URL（/upload?session=新session_id）
    └── 海报码 → 新 URL（后续生图后会填充）
    ↓
④ 前端状态 → IDLE
    ├── 显示待机画面（散粒子漂浮 + 作品名称 + 上传 QR 码）
    ├── 清空之前的所有状态数据
    └── 准备接待下一位用户
```

### B5.3 为什么不用自动倒计时？

之前方案考虑 30 秒自动倒计时→重置，但实际场景中：
- 用户扫码可能需要时间（找手机、打开相机、对焦、保存）
- 有人想多看几眼海报
- 展览节奏不一定需要那么紧凑
- **让用户自己决定什么时候结束**，体验更好

按钮重置的优势：
- 用户掌控节奏，不会急匆匆
- 下一位到了如果当前用户还在看，管理员可以在电脑端手动重置
- 实现更简单（不需要倒计时逻辑）

---

## B6. 网络架构

### B6.1 整体架构（v3 定稿）

> **不再需要便携路由器、局域网 WiFi、iPad。**

```
┌──────────┐     USB      ┌──────────────┐     公网隧道      ┌──────────┐
│  手持按钮  │ ──────────→ │              │ ──────────────→  │          │
│ (HID设备)  │             │   电脑       │   ngrok 或       │  用户手机  │
├──────────┤             │              │   Cloudflare     │  (4G/5G)  │
│ Arduino   │ ──USB串口──→│  Flask 服务   │   Tunnel        │          │
│ (背心)    │             │  (Python)     │                 │  扫QR码   │
└──────────┘             │              │←────────────────→│  访问页面 │
                         │  - SocketIO  │    HTTPS 请求     │          │
                         │  - 上传接口   │                 │          │
                         │  - 海报接口   │                 │          │
                         └──────────────┘                 └──────────┘
```

### B6.2 公网隧道配置

| 方案 | 配置 | 备注 |
|------|------|------|
| **ngrok** | `ngrok http 5000` | 免费版域名每次重启会变，适合开发调试；付费版固定域名 |
| **Cloudflare Tunnel** | `cloudflared tunnel --url http://localhost:5000` | 免费，域名稳定（需要 Cloudflare 账号） |

**推荐 Cloudflare Tunnel**——免费且域名稳定，展览当天不需要担心 QR 码因为重启而变掉。

### B6.3 域名 / URL 格式

| 用途 | URL 示例 |
|------|---------|
| 上传页面 | `https://xxx.trycloudflare.com/upload?s=<session_id>` |
| 海报页面 | `https://xxx.trycloudflare.com/poster/<session_id>` |

QR 码中嵌入完整 URL，用户手机扫码直接在浏览器打开。

---

## B7. 前端状态管理

### B7.1 SHOWING 阶段前端行为

```javascript
// 进入 SHOWING 阶段时的前端状态
state = 'SHOWING';

// 1. 显示 AI 生成的海报图片（通过 <img> 标签加载 /poster/<session_id>）
// 2. 显示海报 QR 码（canvas-qrcode 或类似库渲染）
// 3. 显示提示文字："扫码保存你的专属艺术图"
// 4. 显示提示文字："体验结束后请按按钮"
// 5. 监听按钮事件（SocketIO 'button_press'）
//    → 收到后发送 RESET 请求给 Flask
//    → 切换到 IDLE 状态
```

---

## B8. 降级方案

| 故障场景 | 降级处理 |
|---------|---------|
| 公网隧道不可用 / 断连 | 用户无法扫码上传和保存海报，但**主流程不受影响**——穿背心→按钮启动→引导→呼吸体验→看海报全部正常（投影本地运行）。只是没法把海报带走 |
| AI 生图失败（Gemini API 超时/报错） | 投影显示一张**默认 fallback 海报**（预制的通用粒子脊柱艺术图），不影响用户体验流程 |
| 手机没电 / 不想扫码 | 直接按按钮结束，扫码全程可选 |
| Flask 服务崩溃 | 你作为设计者手动重启服务；投影黑屏时观众自然知道出了问题 |

---

## B9. 总结

### v3 核心要点

1. **双 QR 码，分时出现**：待机时上传码，结束时海报码，每轮自动刷新
2. **海报+QR 码常驻**：不设倒计时，用户慢慢扫码保存
3. **按钮触发重置**：用户按一下 → session 销毁 → 新 session → 回 IDLE
4. **公网隧道**：手机用 4G/5G 即可，不需要 WiFi / 路由器 / iPad
5. **全程无人值守**：一轮结束按钮重置后自动等待下一位
6. **主流程不依赖网络**：即使隧道挂了，核心体验（穿背心→呼吸→看效果）照常进行

### 与旧版（v2）的关键差异

| 项目 | 旧版 v2 | 当前 v3 |
|------|---------|--------|
| 网络方式 | 同一 WiFi + 便携路由器 | 公网隧道（4G/5G 直连） |
| 上传设备 | iPad | 手机 |
| 结束触发 | iPad 点完成体验 / 30 秒倒计时 | 用户按按钮（QR 码常驻） |
| 重置检测 | vest_off（背心脱下检测） | 按钮信号 |
| 是否需要 iPad | ✅ 需要 | ❌ 不需要 |
| 是否需要路由器 | ✅ 需要 | ❌ 不需要 |

---

## B10. 待确认/后续

- [ ] 公网隧道方案最终确认（ngrok vs Cloudflare Tunnel）
- [ ] 海报图片分辨率和尺寸定稿
- [ ] AI 生图的 Prompt 最终调优（需要实际测试效果）
- [ ] 海报页面的 UI 细节（是否加作品名称、设计者署名等）
- [ ] 旧海报文件的清理策略（24h？还是每次重置时清理上一轮的？）
