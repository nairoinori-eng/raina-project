# 启动说明

---

## 第一次使用（只做一次）

### 1. 创建 Python 虚拟环境

```
python3 -m venv ~/raina-venv
```

### 2. 安装 Python 依赖

```
source ~/raina-venv/bin/activate
pip install flask flask-socketio eventlet
```

看到 `Successfully installed` 就完成了。

### 3. 安装前端依赖

```
cd ~/raina-project/spine-viz
npm install
```

---

## 每次启动（开两个终端窗口）

### 终端 A — Flask 后端

```
source ~/raina-venv/bin/activate
cd ~/raina-project
python3 flask_server/app.py
```

看到 `服务启动在 http://localhost:5000` 就对了。

### 终端 B — 前端

```
cd ~/raina-project/spine-viz
npm run dev
```

看到 `Local: http://localhost:5173/` 就对了。

### 打开浏览器

Chrome 地址栏输入：`http://localhost:5173`

---

## 调试面板操作

| 操作 | 效果 |
|------|------|
| 拖动滑块向右 | 脊柱变直，颜色从蓝紫变暖金 |
| 键盘按 D | 隐藏/显示调试面板 |
| 键盘按 F | 全屏 |

---

## 停止服务

在各自终端按 `Control + C`。

---

## 常见问题

**Flask 报错 `No module named 'flask'`**
→ 先运行 `source ~/raina-venv/bin/activate` 再启动

**Flask 报错端口被占用**
→ 系统设置 → 通用 → 隔空投送与接力 → 关掉 AirPlay 接收器

**浏览器打开什么都没有**
→ 确认两个终端都在跑，没有报错
