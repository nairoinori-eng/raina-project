# 启动说明（每次开发前按这个顺序操作）

## 准备工作（只需做一次）

### 安装 Python 依赖

打开终端，输入以下命令并回车：

```
pip3 install flask flask-socketio eventlet
```

等待安装完成（看到 Successfully installed 字样就好了）。

### 安装前端依赖

终端进入 spine-viz 文件夹：

```
cd ~/raina-project/spine-viz
npm install
```

等待安装完成（可能需要 1-2 分钟）。

---

## 每次启动（需要同时开两个终端窗口）

### 终端 A — 启动 Flask 后端

```
cd ~/raina-project
python3 flask_server/app.py
```

看到这行说明启动成功：
```
🟢 [Flask] 服务启动在 http://localhost:5000
```

### 终端 B — 启动前端

```
cd ~/raina-project/spine-viz
npm run dev
```

看到这行说明启动成功：
```
Local:   http://localhost:5173/
```

### 打开浏览器

用 Chrome 打开：http://localhost:5173

你会看到：
- 深色背景上有一条发光的蓝紫色粒子脊柱（弯曲的 S 形）
- 右上角有调试面板

---

## 使用调试面板

| 操作 | 效果 |
|------|------|
| 拖动滑块向右 | 脊柱从弯曲变直，颜色从蓝紫变暖金 |
| 点击「开始体验」 | 进入 GUIDE 引导阶段 |
| 点击「重置」 | 回到 IDLE 待机 |
| 键盘按 D | 隐藏/显示调试面板 |
| 键盘按 F | 全屏（连投影仪时用） |

---

## 停止服务

在各自的终端窗口按 `Control + C` 停止。

---

## 常见问题

**Q：打开浏览器什么都没有？**
确认两个终端里的服务都在跑（没有报错）。

**Q：浏览器报错"无法连接到 Flask"？**
确认终端 A 的 Flask 已经启动，且端口 5000 没有被占用。

**Q：拖滑块没反应？**
确认 Flask 正在运行，浏览器控制台（按 F12）看有没有报错。
